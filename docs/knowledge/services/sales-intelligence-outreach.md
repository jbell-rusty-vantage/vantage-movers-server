---
okf_version: "0.2"
type: Service
title: Outreach and follow-ups
status: active
tags: [sales-intelligence, outreach]
---

# Outreach and follow-ups — CSI-06

September 22 availability repair: five minutes is now a **freshness** threshold, not the lifetime of the latest successful Attention list. Publication stores the new header and any chunks with `expires_at: null`; in the same transaction it assigns older retained snapshots a five-minute TTL for existing cursors. Only cache deletion metadata changes; row contents and snapshot/cursor identities remain immutable. An incomplete or failed build leaves the previous successful list available. `readAttention` returns `status: ready` plus optional `stale: true` after the freshness threshold, with the original `as_of`. Admin displays the delayed-refresh notice and keeps the rows visible. Commands still validate current server state. Before the first successful publication, `pending_projection` remains honest. No index migration or environment change is needed: the existing TTL index ignores null dates. Legacy dated snapshots remain readable until their existing expiry. Rolling back readers removes visibility of retained null-expiry snapshots until the old publisher successfully creates a dated one.

September 21 targeted Outreach backfill: `scripts/backfill-outreach-intelligence.ts` inventories the last 45 days of Form Leads and Call Leads against captured Contact Numbers, excludes official closure and matching Booking records, and selects a bounded mix of both channels. Apply rechecks eligibility, uses the existing attachment/Outreach services, replays attributable calls newest first, and processes at most two distinct recorded interactions per selected opportunity through the existing media/transcription/analysis/application workers. Normal budgets, retries, identity gates and Owner precedence remain authoritative. Reports contain IDs and operational outcomes, not transcripts or customer phone numbers. `--resume` reuses a less-than-24-hour inventory; `--apply` enables writes. No contact messages or official Lead/Booking mutations are performed.

Going cold distinguishes a future agreed deadline from a reminder snooze. Snoozing an already-overdue action postpones that action's Attention deadline but does not suppress Going cold. A genuinely future due/base Attention date still does, including a day-only wait's next staffed opening. Human contact remains the only reset of the cold-contact age.

Attention publication batches 500 Outreach Records, with bounded value-keyed staffing-calendar caches shared across records. This preserves DST and policy-hour behavior without reconstructing the same calendar for each record. Conversation-scoped review items retain their stable subject keys and open the Conversation's actual Contact Number; Number detail includes those reviews. A review whose Conversation no longer supplies a Number stays stored but cannot create a navigable Attention row. The full snapshot remains atomic; the latest successful snapshot stays readable and superseded snapshots retire five minutes after replacement; no partial list is published when the 90-second budget is exceeded. September 21 production-data verification published 6,363 rows in 74.6 seconds; the old path either exhausted its budget or rejected a conversation review as an invalid Lead.

CSI-08 Owner integration: reads add `create_followup` availability, Lead display facts, latest canonical call outcome and actual related official-record links. Host URLs pin `database_scope=production`; API reads still use `scope=production`. Review reads accept `subject_key`, `state` and `cause_kind`, and expose resolution availability. Restriction detail supplies revision and resolution availability. Identity resolution accepts Number or Lead subjects and still rejects unresolved matching ambiguous attachments/identity-review Outreach. Clock scans skip revision/audit writes when neither the aggregate nor an action changed; real wait expiry retains the aggregate fence. Accepted completion/closure notes and command reasons survive in audit history. See [CSI-08 intake](../../call-sales-intelligence/workspace/evidence/csi-08/INTAKE.md).

CSI-14 integration: Outreach detail adds a read-only `nudges` page (20 entries, `/nudges` continuation). Explicit Owner messaging is owned by [Explicit Owner Rep Nudges](sales-intelligence-nudges.md). It reuses this Service's derivation/restrictions/clock rules and appends `nudge_sent` to the Outreach audit subject without completing actions, assigning work, changing dates or reopening records.

CSI-10 integration: `ensure.ts` consumes `repIdentity/resolve.ts` at interaction time, including reviewed retired history. Applied-call dedupe includes identity fingerprints; an unchanged call revision can fill genuinely unassigned work without replacing established/Owner assignments or inferring a promising speaker. Re-evaluation uses existing action completion rules but cannot recreate an already applied missed episode. CSI-10's bounded jobs feed this existing worker; official closure still wins.

CSI-13 integration: [Intelligence application](sales-intelligence-analysis.md) calls the existing `applyOutreachEffect` under a fenced application transaction. Conversation-scoped runs keep their immutable subject; the adapter requires the persisted Outreach binding, exact source conversation/interaction and current attached event identity. Merely likely/candidate and foreign subjects fail, even on the same number. Number Review remains same-number scoped. Effect planning, historical fulfillment, Owner precedence and official closure remain owned here rather than copied into the model runtime.

`applyUnboundContactTypeEffect` supports contact evidence before an Outreach Record exists. It validates the persisted conversation/run/finding/interaction, shares contact-type planning, protects Owner contact-type provenance, updates conversation/interaction and invokes existing `ensureInteraction` transitions. It does not invent an Outreach subject or grant Lead authority. The application worker calls the existing `applySpokenRestriction` directly for source-bound number-only restrictions; uncertain temporary dates remain review.

CSI-13 also coordinates commitment matching here: changed speaker certainty alone cannot create a second source/date obligation. A unique equivalent adds finding provenance without changing the existing promise or responsibility; an explicit captured target is honored rather than ignored. Conflicting kind/date or uncertain description distinctness creates review. Distinct source dates remain independently addressable; resolved obligations never revive.

Runtime: `src/services/salesIntelligence/{outreach,followups,review}/`. Authority: [specification](../../call-sales-intelligence/01-specification.md), [Team C](../../call-sales-intelligence/workspace/teams/c-outreach.md), [handoff](../../call-sales-intelligence/workspace/evidence/csi-06/HANDOFF.md).

`ensure.ts` mirrors official eligibility into one Outreach Record per subject. Official closure cancels outstanding actions and queues number refresh in the same transaction. It wins over identity review. No automatic reopening, official writes or qualification imports occur. CSI-05's transactional hook invokes the identity reaction and its paged fan-out schedules revision-specific Outreach work. Event attribution uses `resolveAtInteraction`, never lifetime edge counts. A Number Review cannot escape ambiguous identity or an officially closed related Lead; rejected unrelated edges do not prevent it.

`transitions.ts` distinguishes attributable attempts, inbound human conversations, provider connection, and missed calls. Only a human conversation with reliably mapped Sales Rep evidence stamps meaningful contact. Reviewed effective-dated identities are consumed read-only; CSI-10 still owns their provisioning/review. Missed episodes retain the first deadline, including out-of-order earlier misses. Later callbacks reconcile historical missed episodes before they can become current outstanding work. Waits, estimates and callbacks remain independent actions.

`staffing.ts` implements Eastern Mon–Sat 08:00–20:00 defaults through persisted policy: 30 staffed minutes first call, 15 missed callback, 1440 Going cold. Day-only waits retain the sales-close promise and use next opening for Attention. Explicit offset timestamps and unambiguous spoken weekday/time forms honor outside-hours promises. Unsupported wording, missing AM/PM, nonexistent/repeated local DST time and nonstaffed day-only dates remain undated. Each obligation stamps policy/date resolution; changing the calendar does not rewrite it.

`followups/commands.ts` uses the trusted Owner command ledger, revision checks, same-transaction instructions/audit, and an aggregate write fence. `applyOwnerCommandInTransaction` is the immediate correction path for Team D. Notes preserve context without implying contact. Overall/action ownership are separate; Owner assignments persist. Snooze changes Attention time while preserving contractual due time; undated snooze is invalid. Close cancels, reopen does not revive obligations, and live official eligibility/permanent suppression still block reopen.

`effects.ts` owns pure planning plus transactional application. Team D supplies validated run-scoped findings and server-resolved intents, never arbitrary updates. Recheck current revisions, event identity, official closure, Owner field instructions and later calls. Source interaction identity and original source date remain separate from completion evidence and the current deadline; repeated extraction resolves the original commitment even after completion or Owner date correction. Multiple possible callback targets require completion review; an actual call cannot silently complete every independent callback. The append-only effect ledger retains outcomes across runs and returns the actual effect target. `review/restrictions.ts` serializes number-level spoken restrictions and preserves Owner resolution across replay. Restrictions block execution without rewriting Owner plans or closing Outreach.

`derive.ts` computes all reasons, the highest-priority band, per-action/record responsibility, human-contact ages, cooldown and blockers using every open action. `reads.ts` exports Owner DTOs with all actions and plural Number detail Outreach rows. Reads do not mutate. Outreach DTO optionally carries `lead_attachment` and `call_progress`; DerivedDto optionally carries `call_state` and `provenance_state`, both derived on read. `certainty_label` comes from `certaintyLabel()`. `/outreach/:id`, `/outreach/by-lead/...`, `/numbers/:id` and `/attention` share those fields. `allowed_actions` includes `start_call` / `end_call` with the same blockers the commands enforce. Blocker codes stay inside `CSI_ERROR_CODES`; both a closed record and a call already in progress return `ILLEGAL_TRANSITION`, and `derived.call_state` is what distinguishes them. `attention.ts` builds immutable snapshots in the worker; GET cursors bind snapshot/filter/as-of and do not create snapshots. An unavailable projection is `pending_projection` with a nullable count, not a false zero. Snapshot build aborts without partial publication at its 90-second budget. Storage uses compression or chunks as described below.

`reactToAttachmentChanged` recomputes a bounded `lead_attachment` + `lead_attachment_revision` display mirror of the deciding `NumberLeadAttachment` edge inside the same attachment-change transaction, via `refreshRecord(..., "outreach_lead_attachment_mirrored", …)`. The mirror follows the record's primary Contact Number only. Closed records are skipped (pre-existing loop). Ambiguous or competing identity is stored as Ambiguous and never picks a Lead. The revision is monotonic on the Contact Number revision: an older change cannot clobber a newer mirror. `decided_by` is `owner` when the edge has Owner `decided_at`, `automatic` when `auto_decision` is present, otherwise `evidence`. Confidence is populated only for an automatic decision.

`call_progress` is Owner Call state on the Outreach Record. It is a separate field; `CSI_OUTREACH_STATES` is untouched and Call state is not a new official Outreach state. `start_call` / `end_call` run through the existing `POST /outreach/:id/commands` path (`recordForUpdate`, Idempotency-Key, `ownerInstruction`, audit). `start_call` writes `call_progress` only: it never changes Outreach state, contacts nobody and completes no action. It is blocked when the record is closed (`ILLEGAL_TRANSITION`), when a call is already in progress (`ILLEGAL_TRANSITION`), and by the same active call restriction that gates dialling (`CONTACT_RESTRICTED`). Starting again after an ended call replaces the field; the audit rows are the history. `ensureInteraction` ends a running call when an attributable Call Interaction covers the started window, using only that interaction's own span (widened backwards by the same duration for the dialling gap) and stamping `interaction_id`.

`worker.ts` registers `outreach_ensure` on the existing job dispatcher, minute cron and stage recovery. Queue payload is only `{job_id}`; current interaction comes from `input_refs`. EntityChange scanning uses `(applied_at,_id)`. All writes remain inside the CSI TEST_MODE database boundary. Flags default off. No routine call/note sends messages, invokes a model, or modifies Lead/Booking/Agent records.

Bounded maintenance (September 21, 2026; [17 §6](../../call-sales-intelligence-new/17-storage-throughput-and-llm-efficiency-handoff.md), [18](../../call-sales-intelligence-new/18-efficiency-implementation-2026-09-21.md)). `runOutreachEnsureOnce` keeps three causes of work separate and every durable job key semantic:

- **Source change.** EntityChange rows for Form/Call Leads raise `outreach-lead:` jobs keyed by the change id, as before.
- **Time boundary.** An indexed query over open `wait` follow-ups whose `due_at` has passed and whose `wait_expired_at` is unset raises one `outreach-clock:` job per boundary (`waitExpiryNomination`: key carries the follow-up id and the exact due instant). A re-dated or snoozed wait is a new boundary. This is the only clock the worker persists: first-action deadlines, follow-up due times, snooze and restriction expiry and Going cold are derived on read by `derive()` and republished by the Attention cron, so no job exists for them.
- **Repair sweep.** Rolling `_id` pages over Form Leads, Call Leads, canonical Call Interactions and **non-closed** Outreach Records (`outreachRepairNomination`). Leads are keyed by their official flags, calls by `projection_revision`, Outreach Records by their own `revision`. The former Outreach Record key carried the sweep-cycle timestamp, so every cycle inserted one completed no-op job per record; that family was about 61,900 of the 90,000 stored jobs. A frozen corpus now hits the dedupe fence on every later sweep and inserts nothing. Closed records are not swept: official closure cannot move them and only an Owner command reopens them.

Attachment fan-out is coalesced: an attachment revision raises one `outreach-number:` job per Contact Number revision (`replayNumberInteractions`), which replays the number's canonical calls in keyset pages of 25 with a continuation job, instead of one `number:` job per call. Per-call `number:` jobs remain the shape of interaction repair and queue wake-ups.

`timeline.ts` registers an append-only CSI audit source with the existing Number Activity timeline merge. Owner notes retain their context; action, assignment, restriction and review events expose prior/current values. Source `happened_at` and audit `recorded_at` remain separate. GET pagination does not write history or projections.


Current availability and analysis-list contract: [September 22 successor](../../call-sales-intelligence/workspace/ATTENTION-AVAILABILITY-SUCCESSOR.md). Locked earlier contracts remain historical; the successor defines the narrow Owner-requested change.

## Attention cache storage (September 21, 2026)

Attention publication validates the complete row array and stores a lossless gzip/base64 encoding when it fits the 12 MB inline budget. The reader accepts both this encoding and the previous inline/chunk representation. Cursor identity, ordering, filters and counts are unchanged. The September 22 availability successor below changes expiry. The decoded payload is capped at 64 MB; larger publications retain the existing chunk path. A measured production snapshot of 6,394 rows decreased from 26,761,829 JSON bytes to 1,242,624 encoded bytes. This matters on the shared 512 MB Atlas cluster: multiple full snapshots had caused storage-quota write failures. Superseded cache documents are disposable after their dated `expires_at`; the latest successful snapshot has no expiry. Customer evidence and audit retention are unchanged. Rollback to a reader without compression or null-expiry support requires publishing a compatible dated snapshot first.

## CSI-15 historical activation

Historical capture persists attachment work but defers Outreach and recording discovery until safe activation. `backfill/readiness.ts` prevents `ensureInteraction` (including generic repair and attachment hooks) from activating a historical call while later planned capture or its number's paged attachment work is incomplete. The activation worker processes only calls in the completed window's half-open `[window_from, window_to)` interval, newest first, ten per fenced continuation, before moving to older source calls. Existing fulfillment and official Booked/Cancelled checks remain the only lifecycle authority; no parallel historical Outreach engine exists.

Missed-call and Intelligence follow-up creation audits retain the source time, and Outreach/Number Review opening uses its original trigger time. Capture time remains observable separately. A successfully completed batch does not spend a transient retry. Retention removes derived follow-up content with a readable tombstone and preserves non-content source/effect identity. Synthetic fulfillment, official-close, chronology and recovery proof: [CSI-15 checks](../../call-sales-intelligence/workspace/evidence/csi-15/CHECKS.md).

## CSI-18 integration (September 19, 2026)

Analysis Owner commands call `applyOwnerCommandInTransaction` for targeted follow-up corrections, eligible cancellation and explicit suggestion application. There is no parallel policy in Admin or MCP. The parent finding/output and exact action revisions are checked before action mutation. Independent actions, nullable dates, later Owner work, completed work, restrictions and official closure retain CSI-06 behavior. The assertion instruction, action audit and eligible review resolution commit with the command result. Original assertions/effects remain immutable; correction outcomes are new attributed history. Validation status is recorded separately for server and UI under `workspace/evidence/csi-18`.


## CSI-16 evidence restamp

Current local certification is recorded in [CSI-16 checks](../../call-sales-intelligence/workspace/evidence/csi-16/CHECKS.md) and the [execution matrix](../../call-sales-intelligence/workspace/ACCEPTANCE.md). CSI-15 backfill/retention/budget recovery is landed on main. Fresh synthetic and isolated browser evidence does not certify production grants or deployed revisions. G4 retains the media Retry-After clock failure; G5 remains partial and the generic conversation replay label fails integration. Exact owners are in [GAPS](../../call-sales-intelligence/workspace/evidence/csi-16/GAPS.md). [G6](../../call-sales-intelligence/workspace/evidence/csi-16/G6.md) is not probed. Owner full rollout follows separately in AFTER-16 D+E; no capability was enabled by this restamp.

## Lead progress, CRM disposition and time sorts (LP-01/03/06, September 22, 2026)

Contract: [Lead progress specification](../../../../sales-intelligence-move-assessment-workspace/SALES-INTELLIGENCE-LEAD-PROGRESS-SPECIFICATION.md) §§3–4, 6, 11, 13–14; agreed interfaces in [LP-01 contract](../../../../sales-intelligence-move-assessment-workspace/evidence/LP-01-CONTRACT.md). Flag `SALES_INTELLIGENCE_LEAD_PROGRESS` (default off). Off keeps `ensureLead` and the repair fingerprint exactly as before.

- **One consumer.** Accepted canonical Lead writes emit an `EntityChange`; the minute scan raises `outreach-lead:` jobs whose `input_refs[1]` is the change id; `ensureLead` is the Lead projection (§13.3 H1). `outreach/leadProgress.ts` is the pure policy: `0` fresh, `1` quoted, `3` rep discretion, `7` CRM bad/unusable, `8` CRM dead opportunity; other valid codes unmapped; missing Unknown. Stored `quoted` is read as its own evidence and never recomputed from the code.
- **Projection.** `OutreachRecord.lead_progress` stores current Priority/Quoted, disposition, `work_observed`, basis (`quoted`, `priority_assigned`, `priority_changed`, `historical_snapshot`), provenance (`accepted` when, judged per field over the last 25 changes touching those paths, a change vouches for the current value; else `uncertain`), the accepted change/observation/decision ids, `source_applied_at` (known accepted time), `first_work_observed_at`, `last_progress_at` (only a semantic change sets it), a semantic `fingerprint` and a `disposition_revision`. An identical re-delivery writes nothing: no save, no audit, no job. `projected_at` is processing time and is never a business time.
- **Transitions (§4 order).** Authoritative closure (`transitions.ts` `authoritativeClosure`: the Lead's official flags or an exact `booked_leads` row, §11.1) → Owner closure/override → identity review → CRM disposition → Lead progress → action-state derivation. Accepted `1`/`3` or `quoted=true` on an eligible Unworked record sets `open`; Band 2 clears because it requires Unworked; Band 5 follows when no action exists, with the secondary reason `no_call_observed`. Nothing sets call timestamps, counters, responsibility, promises or messages. H1a: the "no calls remain" reversion requires `!lead_progress.work_observed`; leaving identity review restores Open when progress exists (`restoreFromIdentityReview`).
- **CRM disposition.** Accepted `7`/`8` closes through `closeRecord(record, "granot_bad_unusable" | "granot_dead_opportunity", "crm_disposition")`: outstanding actions are cancelled with that basis (never completed); official flags, CPL, billing and sheet routing are untouched; no `number_refresh` (paid analysis) nomination is raised for this closure. `7 ↔ 8` refreshes the reason and keeps the closure history. A later nonterminal code queues one deduplicated `disposition_reopen` review and the record stays closed until an explicit Owner `reopen` (which revives no action). A terminal value with uncertain provenance does not close: it opens a `disposition_review`, derive adds the `disposition_review` call blocker, and `create_followup` / `set_waiting` / `start_call` answer `DISPOSITION_REVIEW`. Official and Owner closures keep their stronger reason.
- **Owner commands.** `reopen` on a `crm_disposition` closure is allowed only when the disposition moved on (`CRM_DISPOSITION_CLOSED` otherwise). `override_disposition { disposition_revision, reason }` is revision-scoped: it reopens under the current terminal code, writes an Owner instruction and audit, survives identical redelivery and expires on a semantic disposition change; it never bypasses official closure or a suppressed Number. `mark_worked`, `create_followup`, `set_waiting`, `start_call` answer `CRM_DISPOSITION_CLOSED` while an un-overridden terminal disposition is current.
- **Reads.** `outreachDtoSchema.lead_progress` (optional): raw code, server labels (`priority_label`, `disposition_label`, `basis_label`), provenance, source origin and known time, closure basis, override, reopen review id, `explanation` ("Lead updated in Granot · No next step set" / "Quoted · No next step set" / "Lead updated · No next step set") and `no_call_observed`. `allowed_actions` gains `override_disposition`. `loadAttachedLeadProgressForNumbers` gives the Numbers list the one resolved Lead's progress and Booking, or an explicit `multiple` / `none`; facts are never merged across Leads. The projection is frozen into Attention snapshot rows, so the card and the server band share one `as_of`.
- **Worker (H2/H3).** The entity-change scan re-reads the trailing two minutes on every pass (commit-lag gap) and advances the strict cursor only from the page beyond it; per-change dedupe (`csi:outreach:entity-change:v2:<id>`, versioned because the job now carries the change id) makes re-enqueue a no-op, and a row whose stored payload disagrees is skipped and logged (`outreach.entity_change_job_conflict_skipped`) rather than pinning the cursor. A create or a contact/eligibility path change also raises the `attachment-lead:` job (`leadAttachmentJobInput`), with the 5-minute watermark as backstop. With the flag on, the Lead repair fingerprint includes `granot_priority` and `quoted`, so the sweep recovers a missed accepted change within one lap; off keeps the old key so no mass re-nomination happens before the dry run.
- **Time sorts (§14.1).** `GET /attention` accepts `sort=attention|next_action_due|lead_received|last_human_contact|last_lead_progress`, `direction`, `view=attention`. Publish freezes `sort_keys` per row (`attentionSortKeys`); the read filters, sorts the whole snapshot (nulls last in both directions, ties on `subject_key`), then paginates; the cursor digest binds sort, direction and view for non-default sorts and keeps the previous shape for Attention order. Score sorts and `all_outreach` are added by Move assessment on the same enum. Numbers list sorts live in [Number Activity reads](number-activity-reads.md).
- **Move assessment sorting and all-Outreach view (MA-04).** `GET /attention` adds `sort=transaction_intent|move_likelihood` (default `desc`), `view=attention|all_outreach` and `freshness=fresh|all` (absent = `all`). Publication builds **one** frozen list for both views: band/badge rows as before (`in_attention: true`) plus every other non-purged record whose stored state is not `closed` (`in_attention: false`), built from the same batched inputs/side data; the queued-assessment set is one `distinct` per publish. Each row carries `outreach.move_assessment` (compact projection, applicability applied: closed or accepted un-overridden terminal disposition → `not_applicable` with null scores; `purged`/`failed` → null; `ready` with null → assessed Unknown; queued job → `pending`; nothing → absent = Not assessed) and `sort_keys.transaction_intent/move_likelihood/assessment_status/assessment_stale`. `view=attention` keeps `in_attention !== false` (unmarked older rows stay), so the default population, order and cursor are unchanged. `view=all_outreach` returns every row except `closed` ones unless `state` explicitly includes `closed`; closed records without a band or badge are not in the snapshot at all (their historical scores are read through [Move assessment reads](sales-intelligence-move-assessment.md)). `freshness=fresh` drops `assessment_stale === true`. Score order: filter → global numeric sort → paginate; nulls last both directions, ties on `subject_key`, no band/confidence prefix. Cursor digest: the default request (Attention order, Attention view, no `fresh`) keeps `payloadHash(filters)`; otherwise `payloadHash({ ...filters, sort, direction, view, freshness? })` (`freshness` only when `fresh`, so LP-06 time-sort cursors still resolve); any change is `INVALID_INPUT`. Publish cost grows with the number of open non-band records; measure with `scripts/time-csi-attention-pages.ts` before enabling the Admin score view.
- **Number rollups for the Last human conversation sort.** Capture never labels a call a human conversation; findings (`effects.ts`) and the Owner (`set_contact_type`) do. Each of those writes now queues the Number's durable `rebuild` job in the same transaction when the label crosses the human-conversation boundary (`queueNumberRollupRebuild`), so `rollups.last_human_conversation_at` / `human_conversations_total` follow the label without a second rollup owner. Existing rows need one `enqueueRebuildAll` after deploy.
- **Reconcile.** `scripts/reconcile-lead-progress.ts` (dry run by default) inventories both Lead types and the §13.4 buckets through `completeNormalizedMatchSet`; `--apply --pilot N` / `--apply --resume` runs `ensureLead` in transactional pages of 100 with a checkpoint under `scripts/dev_ops/output/`, policy `lead-progress-v1`, zero model calls, and refuses production without `--allow-production`. Replica proof: `pnpm test:csi:lead-progress:replica` (`scripts/dev_ops/test-csi-lead-progress.replica.test.ts`).
- **Move assessment hooks (MA-02).** The entity-change scan calls `nominateMoveAssessmentForChange` for Lead changes touching move fields (flag `MOVE_ASSESSMENT`), and `closeRecord`/disposition changes need no model call because assessment applicability is derived on read. Details: [Move assessment](sales-intelligence-move-assessment.md).

## Card facts and reference time (S1-FACTS, September 23, 2026)

- **`facts`.** `outreach/facts.ts` `outreachFacts({ record, followups, number, lead, bookings, cancellations, now })` is pure and has two callers through `toOutreachDto`: the Attention publish (frozen on each snapshot row at publish time) and `GET /outreach/:id` (at request time). It returns `{ facts, sort_keys, filter_keys, outcome }` (S2-DESK below). `outreachDtoSchema.facts` is optional so snapshots published before it still parse.
  - `route`: `moveViewsForLead(lead, model).canonical_current`, the same view as Move details; `move_date` is YYYY-MM-DD and Form Leads only; `null` without a Lead (Number-only subjects).
  - `last_call_at`: max of the primary Number's `rollups.last_inbound_at` / `last_outbound_at`. `calls_total`, `conversations_total`, `recordings_available`, `recordings_analyzed` read `interactions_total`, `human_conversations_total`, `recordings_total`, `conversations_analyzed_total`. With no primary Number every one of these is `null` ("No Number on file"), never 0.
  - `newer_call_since_assessment`: `last_call_at > move_assessment.latest_conversation_at`, only when assessment applicability is `active` (the gate `moveAssessmentProjectionDto` uses).
  - `details_disagree`: `move_assessment.conflict_targets` contains `pickup_location`, `delivery_location` or `move_date`; a purged projection never disagrees. The assessment publish writes `conflict_targets` (`artifact.conflicts[].affects`, deduped) with the projection (`projectionFor`); projections published before this read `[]` until their next publication.
  - `next_action_state`: from `record.next_action` (open follow-ups only) with `derive()`'s `attentionDue` and `<= now` boundary, so `overdue` is exactly `derived.overdue` for the action. Closed and identity-review work reports `none`, as `derive()` treats it as not actionable. A snooze postpones the reminder, so a passed contractual `due_at` under a snooze is `due`.
  - `move_date_passed`: `route.move_date < today`, today in America/New_York (flips at ET midnight, not UTC). The same boolean is passed to `moveAssessmentProjectionDto(record, pending, { moveDatePassed })`, which derives the assessment's `stale_reason: "move_date_passed"` on read.
  - `rep_thread`: `null` until rep threads ship.
- **Reference time (data spec §3.8).** Every state above is decided at the caller's `now`: the snapshot's `as_of` for `GET /attention`, request time for `GET /outreach/:id`. The function never reads the process clock; the Admin only formats durations between a server time and `as_of`.
- **Sort keys and the rep filter.** `attentionSortKeys` adds `last_call` and `interactions` from the row's frozen `facts` (null without facts or without a Number); both are selectable sorts since S2-DESK. `agent_id` also matches follow-ups whose `promised_by` is the rep (D7), besides the record and follow-up assignees.
- **`GET /outreach/:id` detail additions (S3-READS, data spec §4.1, final spec §11.1).** `data.outreach` is `outreachDetailDtoSchema` (`outreach/detailDto.ts`): the shared Outreach DTO plus three detail-only fields, never on desk rows. One extra read, in parallel with the existing fan-out: the newest completed, unpurged run of the primary Number (`csi_run_number`, `{createdAt:-1, _id:-1}`, projecting only the overview and summary ids).
  - `newest_run_id`: that run's id, or `null` without a Number, without a completed run, or while the Number's content purge is pending. The analysis page fetches `GET /analysis-runs/:id/presentation` with it.
  - `latest_summary: { run_id, run_kind: "conversation" | "number", completed_at, overview, conversations_covered } | null`: the run's accepted `output.summary.overview` (the step-3 synthesis), redacted and cut by `cutOverview` at the last sentence boundary within 280 characters (then ` …`; a single over-long sentence is cut at a word). `conversations_covered` is `step_artifacts.summaries.length`; a legacy run without step artifacts reports 1 for a conversation run and `null` for a Number run (unknown, never 0). `null` when the run has no usable overview.
  - `official: { status: "open_lead" | "booked" | "cancelled" | "bad_lead" | "duplicate" | "no_sync", booking_id, priority: { code, label } | null } | null`: `officialClosure(lead)` over the widened Lead projection, then the exact `booked_leads` / `cancelled_leads` rows already loaded (as `authoritativeClosure` does, without its extra reads). `booking_id` is the Lead's newest Booking. `priority` is the Lead's Granot Priority with `priorityLabel`. `null` for a Number-only subject or a missing Lead.
- **Widened publish reads (§3.2), still batched per page.** The Lead `$in` projects the move fields (`pickup_*`, `delivery_*`, `destination_zip` for Form, `delivery_zip` for Call, `move_date`, `move_size`, `granot_move_size`, `cubic_feet`, `current_move_provenance`), `timestamp`, the official flags (`booked`, `cancelled`, `duplicate`, `bad_lead`, `no_sync`), `granot_priority`, `quoted` and `receiver_agent_name_snapshot`. Bookings add `book_date, total_binder_amount, job_no, agent`; cancellations add `cancel_date, reason`; the one agents `$in` now runs after the bookings and includes booking agents. The agents, cancellations and attachment reads run in parallel. Nothing is read per row: the replica proof (`scripts/dev_ops/test-si-facts.ts`) counts the same 15 reads for a 10-record and a 60-record publish. The `latest_number_call` aggregation stays while the production Admin still reads it (data spec D1, deferred).

## Desk model: filters, closed partition, metrics, index (S2-DESK, September 23, 2026)

Data spec §3.4–§3.7, final spec §6–§8. `outreach/attention.ts`, `outreach/attentionIndex.ts`, `outreach/facts.ts`, `dto.ts`, snapshot header in `models/salesIntelligence/infrastructure.ts`.

- **Flag `SALES_INTELLIGENCE_ATTENTION_V2` (default off).** On: the publish keeps the closed partition and writes `metrics` and `index_gzip_base64` on the header. Off: the publish is today's (no closed rows, no metrics, no index). In both modes rows carry the additive `partition` and `filter_keys`, so the new filters and sorts work either way; the production Admin (539a628) strips unknown keys and never sends the new parameters, which the replica proof checks by parsing every flag-off and flag-on response with a verbatim copy of its `attentionSchema` (`outreach/attention.adminSchema.fixture.ts`). The read is snapshot-driven, not flag-driven: a header with an index is read through it, a header without one is read as before. `view=closed` on a flag-off snapshot is an empty page. Until `ATTENTION_V2` joins `CSI_FLAGS`, `attentionV2Enabled()` reads the variable directly.
- **`filter_keys` (row, §2.3).** `recordFilterKeys` (in `facts.ts`, returned by `outreachFacts`): `agents` = record assignee ∪ every follow-up's responsible agent ∪ `promised_by` (V10, deduped, sorted), `attachment` (`lead`/`none`), `priority` (raw Granot code from `lead_progress`, `null` = Not set), `has_recording` (`recordings_available > 0`), `has_assessment` (status `ready`/`insufficient_evidence` and applicability `active`), `newer_call`, `ti`/`ml` (the frozen score keys), `received_at` (`sort_keys.lead_received`), `move_date` (`facts.route.move_date`), `outcome`, `closed_at`. The publish adds `band`, `needs_review` and `state` from the derived row (`attentionFilterKeys`).
- **Parameters (§3.4).** `band[]`, `state[]`, `needs_review`, `agent_id[]`, `unassigned`, `attachment`, `priority[]` (`not_set` = null), `has_recording`, `has_assessment`, `newer_call`, `ti_min`, `ml_min`, `received_from`, `received_to`, `move_date_within` (days: move date in [today, today + n], ET calendar day of the snapshot `as_of`), `move_date_passed`, and with `view=closed` only: `outcome[]`, `closed_from`, `closed_to`. Bounds are half-open `[from, to)`; a null key never matches a bound, a minimum or a boolean. `rep_unread` (Phase 5) is rejected as an unknown parameter. Closed-only parameters and sorts outside `view=closed` are `INVALID_INPUT` rather than ignored. No new parameter has a default, so a request without them hashes to the same cursor digest as before; every new parameter is bound by the digest.
- **Sorts.** `last_call` (desc), `interactions` (desc); in `view=closed` also `closed` (desc) and `time_to_close` (asc). Nulls last both directions, ties on `subject_key`. `attention` in the closed view is the stored order, newest closure first.
- **Closed partition (§3.5, flag on).** A record with stored state `closed`, `closed_at ≥ as_of − 90d` and an outcome is emitted with `partition: "closed"`, `in_attention: false`, `outreach.followups: []`, `outreach.allowed_actions: []`, `allowed_actions: []`, `outcome` and `sort_keys.closed/time_to_close`. Outcomes: official `booked`, `cancelled`, `bad_lead`, `duplicate`, `no_sync`; CRM `granot_dead_opportunity` → `crm_dead`, `granot_bad_unusable` → `crm_bad_unusable` (with `priority {code, label}`); origin `owner` → `owner` (`note` = the Owner's reason text). `lead_context_available` (Number review, `ensure.ts`) and `lead_unavailable` (command path) are official closures but not outcomes and are excluded. `time_to_close_ms`: booked `book_date − trigger_at`, cancelled `cancel_date − trigger_at`, otherwise `closed_at − trigger_at`; null when that instant is missing (a booked record without its Booking row), clamped at 0 when a date-only Booking precedes the Lead's arrival. A closed record that also carries a review badge keeps its in-Attention row and is emitted in the partition as well; views never mix partitions (`attention` and `all_outreach` never return closed-partition rows), so the Admin sees each subject once per view.
- **Metrics (§3.6, flag on).** `attentionMetrics` over the publish's rows plus the walk count: `leads_received_7d` (every walked non-purged record, any state, `trigger_kind = lead_arrival`, `trigger_at ≥ as_of − 7d`), `not_called_yet` / `callbacks_overdue` (active-partition, non-closed rows in band 2 / 1), `awaiting_assessment` (same rows with `newer_call_since_assessment`, or `conversations_total ≥ 1` and assessment `pending`/not assessed), `booked_7d` and `booked_7d_median_days` (closed partition, outcome `booked`, `closed_at ≥ as_of − 7d`; whole days rounded down of the median `time_to_close_ms`; null without a booking). `GET /attention` returns them as `data.metrics` only when the header has them.
- **Index (§3.7 / D9, flag on).** `index_gzip_base64` holds `{ subject_key, partition, in_attention, sort_keys, filter_keys, reasons, chunk_index, position }` per stored row. The read decodes the index, filters, sorts, counts (`total_items`, `reason_counts`) and slices over it, then materializes only the page: inline snapshots decode the row array once; chunked snapshots load only the chunk siblings the page names (`csi_attention_parent_chunk`). A page row whose `subject_key` differs from its index entry fails the read instead of returning a wrong page.
- **Cost (measured, `scripts/dev_ops/measure-si-attention.ts`).** See the S2-DESK evidence file for the walk time against the 90-second budget and `GET /attention` p50/p95 at production volume.
- **Proof.** Unit: `outreach/attentionIndex.test.ts` (B5 table over every parameter, views, ET-midnight and DST move-date boundaries, sorts, query contract, digest compatibility, B7 recount, index round trip, Admin-schema parse), `outreach/facts.test.ts` (filter keys, B6 time-to-close rules, outcome mapping and exclusions). Replica: `node --import tsx scripts/dev_ops/test-si-desk.ts` (`testvantagemovers_s2desk`: flag off vs on, B6, B7, paging, chunked reads, a pre-S2 snapshot).
