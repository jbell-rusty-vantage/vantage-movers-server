# Team handoff — CSI-06

- **Team / issue / date:** C / CSI-06 only / September 18, 2026.
- **Agent / repo / branch / baseline:** Codex; `vantage-main-server` / `sales-intelligence` / `999c63d`. Clean expected baseline verified before claim. Dashboard untouched. No branch switch, commit or push.
- **Concrete behavior:** Outreach ensure/official closure; event-time attribution; inbound-human/outbound-attempt transitions; multiple independent nullable-due actions; overall/action ownership; Owner commands and instructions; staffed clocks, waits and missed episodes; restrictions/review; authoritative planning/application and fingerprints; read-only plural DTOs and immutable Attention pagination; registered queue, cron, recovery and applied-at scans.
- **Owned / changed files:** [FILES.md](FILES.md). New Outreach/followups/review services and proofs; additive shared model/source-reference fields, DTOs, hook, route/queue/cron/package/Vercel registration. A small capture compatibility fix preserves Owner `unknown` contact-type corrections. No CSI-05 suggestion/command reimplementation.
- **Contracts / consumers:** `csi-outreach-v1` in [CONTRACTS.md](../../CONTRACTS.md#csi-06-concrete-imports-server-relative-september-18). D consumes `planOutreachEffect` / `applyOutreachEffect` and immediate `applyOwnerCommandInTransaction`. Number-only work uses the shared eligibility/restriction primitives in the same application transaction. E consumes server DTOs/commands/Attention; CSI-07 uses audit and clock invalidations; CSI-09 uses stamped policy clocks. No external messages sent.
- **Checks / artifacts:** [CHECKS.md](CHECKS.md) and captured logs. Current typecheck/lint pass; 31 focused and 22 Outreach replica tests pass; read/attachment/fan-out regressions pass 10/11/11. Full source suite passed 2392/114 skipped before final affected-source reruns. Required checkpoint failed on its earlier snapshot; reported type/doc issues are fixed in current source. No checkpoint patch applied or final independent approval claimed; [review record](REVIEW.md). Never infer deployment proof from synthetic tests.
- **Races / idempotency / failure:** command replay/payload conflict; aggregate/action revisions; worker lease expiry rollback; duplicate wake-ups; exact missed deadline; source-versus-completion identity; repeated promise after fulfillment and Owner correction; official closure during identity resolution; historical callback and Booking reconciliation; read-only GET and snapshot pagination; flag-off no-op.
- **Known gaps / capability blockers:** production scale and live queue/cron remain unverified. Attention snapshot publication is bounded by 40 seconds/12 MB and does not publish truncated rows; failed publication leaves the prior snapshot until expiry, then explicit pending projection. Reviewed mappings are consumed but never created here; unknown identity stays unassigned until CSI-10. Date parsing intentionally accepts a small deterministic vocabulary; unsupported wording stays undated. D still owns evidence authorization, model runs/submission, findings publication and the outer application-job lease; this issue adds no agent/STT/MCP runtime. The interfaces above are server primitives, not model-callable mutation tools.
- **Deployment actions:** none. No production migration, subscription, provider call, live message, secrets, flag enablement or official Lead/Booking/Agent mutation. Existing CSI unique indexes are prerequisites; additive nullable source fields require no new index. Rollback keeps OUTREACH_ENSURE off while preserving history.
- **Next dependency / exact entry point:** D starts with `src/services/salesIntelligence/outreach/effects.ts` and `followups/commands.ts`; E starts with `outreach/reads.ts`, `outreach/attention.ts` and the registered Owner routes. CSI-10 and CSI-14 remain not started.
- **Ledger:** CSI-06 claim and evidence updated; unrelated issue status is unchanged.
