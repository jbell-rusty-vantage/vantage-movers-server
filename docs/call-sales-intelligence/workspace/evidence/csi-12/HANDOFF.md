# Team handoff — CSI-12

- **Team / issue / date:** B / CSI-12 / September 18, 2026.
- **Agent / repo / branch / baseline:** Codex; `vantage-main-server`, `sales-intelligence`, `63b3dfb` (CSI-11 committed). Clean working tree verified before claim; no branch switch, commit or push.
- **Concrete behavior:** durable scheduling from CSI-11 stored-media hooks; one bounded private Blob/Gateway STT unit with budget admission; redaction before any persistence including provider boundaries; immutable transcript version and latest cache; actual cost reconciliation; durable analysis intent with completion eligibility recheck; stage-specific replay, pauses and eight-failure recovery. Seed media and account uniqueness preserved.
- **Files owned / changed:** [FILES.md](FILES.md). Additive shared changes limited to config/model/read DTO/budget/policy/queue/cron, plus scoped tests, Service docs/catalog/rule and coordination documents. No Admin/MCP/qualification/official record implementation.
- **Contracts / consumers:** [csi-transcript-v1 imports](../../CONTRACTS.md#csi-12-concrete-imports-server-relative-september-18). D receives conversation + immutable snapshot IDs in `analysis.input_refs`; snapshot contains version/segments/media digest/pricing. Undetermined completion is paused analysis intent; excluded completion suppresses analysis. This written packet is the handoff; no messages sent to external teams.
- **Checks / artifacts:** [CHECKS.md](CHECKS.md) records exact executed commands and outcomes. Separate reviewer assessed and re-reviewed all implementation and checkpoint findings; [review](INDEPENDENT-REVIEW.md). Final quality checkpoint results recorded in CHECKS.
- **Race/idempotency/failure proof:** synthetic STT/private Blob bytes plus disposable loopback Mongo replica. Budget pause/resume through actual Owner policy, unknown timing/speaker, split redaction, Mongo/log privacy, immutable replay, stage separation, eligibility flips, ambiguous spend, eight failures, killed eighth claim, competing claims, expired lease, bounded concurrent scheduling and changed-media versions. CSI-01–04/11 replicas and qualification regressions run separately.
- **Known limitations:** no live Gateway/Blob/RingCentral proof. Missing billed cost keeps successful redacted evidence and an unresolved reservation (`stt_cost_unreported`, nullable STT cost); ambiguous provider failures retain reservations for Team F cost recovery. Admission/actual accounting uses upward integer-cent rounding. A crash between remote success and durable completion can repeat a provider request; no remote exactly-once guarantee is asserted. Explicit regeneration commands are outside CSI-12.
- **Deployment actions:** none. Flags stay off. Configure verified positive `SALES_INTELLIGENCE_STT_CENTS_PER_SECOND` before STT enablement, plus existing Gateway/Blob/cron/dataset credentials and active budget period. Model defaults to proposed `openai/gpt-4o-mini-transcribe`; configurable. Missing price/duration pauses, never guesses. No new indexes or migration; CSI-01 index/account prerequisites remain. Rollback keeps STT off and preserves snapshots, jobs and reservations. Seed compatibility retained; server cost DTO now allows null STT actual.
- **Next dependency:** CSI-13 / Team D consumes `analysis` jobs via `getIntelligenceEvidenceSnapshotModel()` and `input_refs[1]`; validate live eligibility and consumed version before analysis. Do not retranscribe for an analysis retry. No analysis handler is registered here. F verifies production route/cost/privacy compatibility and outstanding-reservation recovery.
- **Ledger:** CSI-12 claim and shared-file ownership recorded; CSI-11 baseline clarified as committed `63b3dfb`. Other team readiness unchanged.
