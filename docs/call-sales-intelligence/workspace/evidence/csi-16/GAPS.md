# CSI-16 owning-issue findings

These are filed in the workspace issue ledger used by this project, not silently fixed by changing Teams B–E fixtures. They remain open after the certification packet. Owner's accepted full-rollout plan is unchanged.

| Finding | Owner / issue | Trigger and evidence | Required follow-up |
| --- | --- | --- | --- |
| F-01: media Retry-After clock assertion fails | Team B CSI-11, shared Team A CSI-01 jobs seam | Fresh `test-csi-media.ts`: 18 pass / 2 fail including enclosing parent. Leaf `429 honors Retry-After or default without burning failure attempts`, `scripts/test-csi-media.replica.test.ts:164`: expected `1789864002851`, actual `1789864002891` (+40 ms). `media.ts` injects `now` but throttled `failCsiJob` calls omit absolute `resumeAt`; `jobs.ts:249` uses real `Date.now()`. | Resolve the injected-clock versus failure-clock contract with owning teams and rerun the unchanged media regression. The mismatch alone does not prove early retry or customer impact. No fixture patched; G4 is not wholly green. |
| F-02: unconditional replay claim | Team E CSI-07, CSI-16 codebase integration | `vantage-admin/components/conversations/conversation-panel.tsx:138` always says the run replayed already-paid artifacts; `tests/conversations-page.test.ts:173` expects it without per-run provenance. | Make provenance copy reflect actual run data; validate generic live conversation rendering. Source assertion fails even though synthetic conversation tests expect it. No claim of current browser audio/model proof. |
| F-03: full server typecheck blocked by external probe | Concurrent external MCP probe owner, outside CSI-16 | Three TS18046 errors at `scripts/probe-intelligence-mcp-agent.ts:231`; scoped check excludes only that external file in addition to repository's original exclusion. | Probe owner narrows unknown content; preserve this task's dirt exclusion. Full typecheck remains failed until then. |
| F-04: full message dialog/history and actual delivery unproved | Team E/C CSI-14; AFTER-16 B/G | Smallest directory-User picker and destination/P2 exist; synthetic nudge replica 18/18; no live send or full send-history walkthrough. | Existing later slice, not a new implementation wave. |
| F-05: empty-recording re-evaluation gate | Team C/B CSI-10; AFTER-16 A | `repIdentity/worker.ts:41` still requires `call.recordings.length` before discovery. | Existing narrow repair and regression; not implemented here. |
| F-06: production capabilities unprobed | Team F/Owner operations; AFTER-16 D+E | [G6](G6.md); historical recording denial and September 19 FEATURE_DISABLED are not current grants or readiness. | Read-only capability probe in cutover session, then Owner-authorized full rollout. No CSI-16 green declaration substitutes for this. |

Named subjects remain Lead Outreach-only historical examples. The local read checks correctly return 404 for their production IDs rather than importing production data. Browser capture/attach/Play for them is not run.

## F-07 — pending-recording delay is computed but discarded

Owner: **Team B / CSI-11**, shared **Team A / CSI-01 jobs** seam. Source-confirmed by the required Composer checkpoint first review and coordinator inspection; not a new failing assertion in the unchanged replica suite. On content 404 inside the availability window, `media.ts:134` computes `recordingPendingDelay(firstObserved, now)` (60 seconds increasing to 1 hour), but lines 136–137 pass neither that delay nor an absolute resumeAt for recording_pending. `jobs.ts:244–249` instead uses its generic 30-second exponential/jitter delay and real clock. Deferred pending claims refund their attempt, so ordinary first-claim retries do not progress through the intended age-based policy. Existing 404 tests force due() and do not verify this timestamp.

Impact: pending recording retry scheduling differs from the stated CSI-11 engineering contract and can schedule more frequent provider checks. No production traffic impact was measured. Required follow-up: preserve computed pending/throttle resume time through the existing job seam, add a focused timestamp assertion, and rerun unchanged existing media proofs. No patch applied in CSI-16. This is separate from F-01's exact 429 clock mismatch.
