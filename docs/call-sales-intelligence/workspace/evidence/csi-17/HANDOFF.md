# CSI-17 handoff

- Team / issue / date: Team D, CSI-17 plus explicit Owner additions, September 18–19, 2026.
- Agent/repositories: coordinating Codex with authorized GPT-6 Astra MCP agent and scoped-read agent. Server `sales-intelligence`, HEAD `3355c6dcf68ed2c092824dc1dbb87c71a3f0c343`, remote `https://github.com/jbell-rusty-vantage/vantage-movers-server.git`. Existing dirty CSI-10/14 work preserved. MCP initially clean `main`, HEAD `7e12815189f24d422873f54f146f1fe1033dd655`, remote `https://github.com/jbell-rusty-vantage/vantage-movers-mcp.git`; exact authorized local `sales-intelligence` branch created. Both remotes belong to `jbell-rusty-vantage`. No commit/PR/push; dashboard untouched.
- Concrete behavior: dedicated MCP endpoint exposes twelve closed tools plus pinned prompt/schema. Server performs relevant bounded reads, captures immutable redacted responses and admits one durable strict envelope. Operational datasets cover Form/Call Leads, Bookings/Cancellations, Job Number timeline, Agents, Granot CRM Sources, RingCentral queues/Users. Two provider tools use fixed read-only Call Log adapter, default off and fake-tested. No arbitrary Mongo query/filter/aggregation, messaging or official-write authority.
- Files owned/changed: server `analysis/{contracts,lease,run,capture,submit,reads,operational}.ts`, unit tests, internal router, two replica runners/suites, schema generator, owning Service/catalog, CSI-17 evidence. Coordinated additions to envelope citation enum, CSI tools/errors/flag config, run/submission model fields, job evidence fence, shared multi-record evidence validation, v1 registration and package scripts. MCP dedicated route, `lib/intelligence/**`, broad endpoint intelligence denial, tests and README/CONTEXT/docs. Exact tracked/untracked inventories are captured in `FILES.md`; prior dirty files are not all CSI-17-owned.
- Contract/version/consumers: [API-CONTRACT.md](API-CONTRACT.md) is the concrete handoff. `csi-intelligence-v1`, `sales_intelligence_analyze_v1`, strict `csi-envelope-v1` with Owner-authorized additive citation kinds, canonical digest `18a4746f6343c4b093ef449693b685872e3f912ae36fe87e4d49196fc1ecfed7`. Generator and direct cross-repository parity tests prevent independent schema drift. Server-only checkpoint snapshots intentionally skip the cross-repo artifact check; direct workspace checks execute it.
- Tests: [CHECKS.md](CHECKS.md), [READS-CHECKS.md](READS-CHECKS.md), [MCP-CHECKS.md](MCP-CHECKS.md) distinguish current direct tests from checkpoint results and intentionally unexecuted live proof. The required no-apply quality checkpoint is recorded separately; no blanket independent approval is inferred from a prior snapshot.
- Race/failure behavior: concurrent duplicate reads reuse one snapshot; duplicate submission produces one immutable receipt and one application intent. Transaction rollback leaves no submission/audit/application intent; timeout after durable commit recovers the same receipt. Run revision and active-lease fence prevent late evidence after finalization. Original-evidence mode replays retained prompt/responses and forbids current expansion; missing retained evidence fails closed. Unknown/historical/proposed/conflicting identity and incomplete transcript coverage remain explicit data.
- Defaults/rollback: all deployment flags unchanged; provider reads default off. Only child proof processes enable synthetic flags. To withdraw capability, leave core/provider flags off and omit the dedicated MCP route/key configuration. Captured evidence/submissions and paused intents should be preserved, never removed as a rollback shortcut. No provider configuration, credential, subscription or production migration is provisioned.
- Known gaps/downstream: CSI-13 ToolLoopAgent, budgets around actual invocation, scheduling, application/effects and number synthesis are absent. Its application job is durably `paused/consumer_unavailable`, never applied and never burns retries. CSI-18 correction selection/UI is absent (`owner_correction_ids` must be empty). Exact quotation/location/entailment verification is intentionally deferred. Directory/Agent records and search candidates do not establish a reviewed speaker, confirmed attachment or send authority. Oversized scope can fail closed instead of hiding Owner context.
- Prior review boundaries: fresh CSI-14 independent P2 concerning editable review-context body bypass of call restrictions remains unresolved and separate; no messaging fix is included. CSI-06's failed old checkpoint, CSI-10's passing-but-stale snapshot and CSI-14's two failed final snapshot reviews remain historical; later direct checks do not erase those findings. No whole-app independent approval is claimed.
- CSI-17 checkpoint: required no-apply run `1789786268819-98e1193c` finished **failed/verify**, with two local dependency file-read failures in its offline suite; targeted retries in the same snapshot passed. It proposed a separate CSI-10 empty-recording replay fix, which was inspected and left unapplied. Final direct source typecheck/lint, offline suite (2426 pass, 114 skipped), MCP35/build and scoped/required replica suites passed. See CHECKS for exact fingerprints, earlier snapshot scope and preserved outputs; no independent final approval is claimed.
- Deployment actions: none. Only existing loopback disposable replica `csi01` container was restarted; random `testvantagemovers_csi17*` databases were created and dropped by guarded proof runners. No live model/STT/Blob/RingCentral, deployed MCP, production API/database/media, send, backfill, deployment, flag enablement, commit or push.
- Next dependency: CSI-13 imports `prepareIntelligenceRun(lease,input)`, fetches exact MCP prompt and schema, consumes CSI-12 pinned transcript through tools, invokes its separately implemented loop, gets/recover one receipt, then implements a real application consumer with current-state/Owner revalidation. `recoverIntelligenceSubmission` supports trusted renewed-lease recovery without loosening HTTP auth.
- Ledger: CSI-17 ownership and final implementation/check status recorded; CSI-13/18 remain separate. See MCP documentation for synthetic transport fixtures and forbidden-call examples.
