# CSI-14 handoff

- Team / issue / date: Team C, CSI-14 server, September 18, 2026. Team E retains Admin dialogs.
- Agent and repo / branch / commit or PR: Codex; `vantage-main-server`, `sales-intelligence`; observed baseline `3355c6dcf68ed2c092824dc1dbb87c71a3f0c343`, remote `https://github.com/jbell-rusty-vantage/vantage-movers-server.git`. The initial checkout contained modified/untracked CSI-10 work. It was preserved. No source commit, branch switch or push.
- Concrete behavior delivered: signed Owner preview/send/history, current reviewed identity authority, versioned masked templates/edited-body validation, customer-destination guards, explicit optional pager fallback, durable idempotent intent/submission/receipt lifecycle, concurrent per-link rate admission, authenticated bounded repair with expired-lease/orphan recovery, and read-only Outreach detail history. No dashboard/MCP/automatic/customer sends.
- Files owned and changed: `src/services/salesIntelligence/nudges/{adapters,commands,eligibility,reads,repair,templates}.ts`, focused unit/route tests, `scripts/test-csi-nudges{,.replica.test}.ts`, this evidence folder and owning `sales-intelligence-nudges.md` Service. Coordinated additive files: `OwnerRepNudge.ts`, CSI configuration/validation/audit infrastructure, `transactions.ts` audit kind, `policy.ts` optional session, Owner/cron routers, `numberActivity/jobDispatch.ts`, Outreach detail reader and nudge timeline invalidation, CSI-10 rep DTO, `package.json`, `vercel.json`, shared docs/catalog/ledger/contracts. Existing CSI-10 edits in these files are not attributed to CSI-14.
- Contract/version changes and consumers notified: [concrete imports and browser contract](API-CONTRACT.md), also indexed in `workspace/CONTRACTS.md`. Team E and CSI-08/09 consume preview/send/history; CSI-06 consumes the nudge page/audit subject; CSI-10 supplies authority; operations owns deployment/current provider grants; CSI-13 retains explicit no-send boundary. No UI/browser implementation claimed.
- Tests/checks actually run: [CHECKS.md](CHECKS.md) records exact commands and output files. [REVIEW.md](REVIEW.md) records quality checkpoint input/status and separates snapshot review from direct current-source checks.
- Race/idempotency/failure cases verified: concurrent duplicate Owner commands submit once; changed payload/stale revisions conflict; pending/unknown replay never submits; current identity changes between preview/chat preparation stop submission; all persisted attempts reserve the hourly limit; definitive chat failure alone permits explicitly authorized pager; acceptance timeout/persistence failure produces uncertainty; exact receipt repair, duplicate repair, expired leases, denied reads, and bounded orphan recovery do not call submit. Legacy pending records lacking a revision terminalize without resubmission; Team Messaging receipts must match both the exact message ID and requested Direct chat. See synthetic replica fixtures for exact schedules and assertions.
- Known gaps or capability blockers: no live provider grants or acceptance are proven. Historical read-only capability evidence is not current send permission. Exactly-once delivery is not claimed. A separately approved live proof must use one Owner-selected reviewed rep and approved copy. Team E dialogs/CSI-08 integration remain separate. Provider acceptance does not establish reading, calling, customer contact, completed work or a promise. Capped/incomplete customer evidence blocks rather than authorizes.
- Deployment actions performed: none. No credentials loaded for tests, provider probes, production migration/backfill, flag enablement, commit or push. Tests use injected fakes and a random isolated database on the existing loopback `csi01` replica, dropped in teardown.
- Configuration/rollback: master+nudge flags and persisted `nudges` capability required; SMS/pager default off, Team Messaging primary; hourly limit six. [Configuration table](API-CONTRACT.md#configuration-and-operations). Disable nudge/channel flags to stop new sends and repair; keep records/commands/audits. Owner history remains readable under the master flag. Re-enable repair only to reconcile existing intent, never to resend unknown delivery.
- Next dependency and exact entry point: Team E consumes POST `/api/v1/admin/sales-intelligence/nudges/preview`, POST `/nudges`, GET `/nudges` and `GET /outreach/:id` → `data.nudges`. Use returned revisions and a stable idempotency key; show pending/unknown semantics from the DTO. Operations uses `/api/cron/sales-intelligence-nudge-repair` with existing cron authentication; no browser repair endpoint exists.
- Ledger rows updated: CSI-14 ownership claimed before implementation; final delivery/checkpoint status maintained in `workspace/LEDGER.md`.

## Reproducible proof

Run `pnpm test:csi:nudges:replica` with the existing loopback replica at `127.0.0.1:27189`, set name `csi01`. The wrapper strips inherited RingCentral/CSI environment values, sets synthetic identities, `555-01xx` numbers and isolated test-only flags, refuses a nonrandom/nonloopback target, and never loads `.env`. The default production adapter refuses provider access in TEST_MODE. HTTP unit tests use loopback Express and signed synthetic Owner requests.

Synthetic browser request/response and error examples: [API-CONTRACT.md](API-CONTRACT.md). Actual persisted sent body is masked; the test proves official Lead, Agent, reviewed link, directory, Outreach and action snapshots remain unchanged by sending.

## Preceding checkpoints

CSI-10's isolated checkpoint checks/final review passed, but its overall result became stale after source refinements. Subsequent direct checks passed; that is **not independent final approval of final current source**. CSI-06 likewise has an older failed checkpoint snapshot followed by passing direct source checks. CSI-14 preserves these distinctions and does not retroactively approve either snapshot.


## Final delivery status

Server implementation, contracts, integration, fake-provider verification and handoff are complete. Current source passes typecheck/lint, 31 focused tests, 16 nudge replica tests and the full offline suite (2409 pass, 114 skipped), plus the related replica regressions. Both required isolated checkpoint runs passed their automated checks but failed final review on snapshot documentation contradictions, since corrected in current source. These remain failed snapshots; no independent final current-source approval is claimed. The separately gated live-send proof is intentionally outstanding.

## Subsequent Owner-requested independent review

GPT-6 Astra reviewed the current source after this handoff and found one verified unresolved P2: edited review-context text can bypass the call-suggestion purpose gate. Existing nudge unit/route tests passed 11/11 but omit the demonstrated cases. CSI-14 is now recorded as review with one unresolved finding, not cleanly approved. See [INDEPENDENT-REVIEW.md](INDEPENDENT-REVIEW.md) for reproduction and scope; [NEXT-ISSUE-CSI-17.md](NEXT-ISSUE-CSI-17.md) is the separate next-feature implementation brief. No runtime fix was made during this review/documentation request.
