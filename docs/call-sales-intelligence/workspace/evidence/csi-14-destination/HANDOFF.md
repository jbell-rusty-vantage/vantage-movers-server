# Team handoff — CSI-14 directory-User destinations and P2

- **Team / issue / date:** C + smallest E picker / CSI-14 destination + P2 / September 19, 2026.
- **Agent / repo / branch / baseline:** Cursor Grok 4.6; server `sales-intelligence` HEAD `bbdfe4c2ea080f4a962d74913baed0846c1ac763` (dirty CSI-14 dest docs + CSI-10 seed preserved); Admin `sales-intelligence` HEAD `905fe8777e5707f20b32b2a76c59a3566dafaf09` (dirty CSI-10 seed UI preserved). Remotes `jbell-rusty-vantage`. No branch switch. No commit/push.
- **Concrete behavior:** Preview/send require `rc_account_id` + `rc_extension_id` on a current stored-directory User. A reviewed Rep Identity Link is optional metadata and must travel with `expected_rep_revision`. Rate admission is per `(rc_account_id, rc_extension_id)`. Channels come from the snapshot User (pager when an extension number exists; `sms_to_rep` only for exactly one stored DID; Team Messaging only when a stored person id already exists) and a reviewed link may only restrict them. Recipient first name comes from the directory User unless a current reviewed link supplies an Agent name. Edited `review_context` bodies fail closed on contact instructions before any provider adapter call. Admin Message-rep lists current directory Users, including unmatched Users.
- **Owned/changed files:** [FILES.md](FILES.md).
- **Contracts / consumers:** [API-CONTRACT destination addendum](../csi-14/API-CONTRACT.md), [CONTRACTS](../../CONTRACTS.md).
- **Checks / artifacts:** [CHECKS.md](CHECKS.md), [REVIEW.md](REVIEW.md). Source is this checkout. Isolated Composer 2.5 checkpoint `1789834716855-f5ff3f75` was patch-ready / PASS and was not applied wholesale. Customer-destination rejection now records an Outreach operational event.
- **Race / replay / recovery:** existing durable intent, submission boundary, and repair-only recovery reused. All persisted states still reserve the rolling hour.
- **Known limits:** Team Messaging remains unavailable unless a stored person id already exists; snapshot Users have no person id field, so unmatched Users are pager/SMS only when those snapshot facts exist. Official `migration:csi:indexes` was not applied; `nudge_extension_created` is defined on the model and created in the disposable replica. `NUDGE_ENABLED` stayed off outside the isolated replica wrapper. No live send. No identity writes for Josh, Roy, Jason, either Tyler, Russell, QA, or unmatched Users.
- **Deployment actions:** none.
- **Next:** fuller CSI-14 dialogs/history, CSI-09, separately authorized live send. CSI-15 is not this work.
