# Team handoff — CSI-11

- **Team / issue / date:** B / CSI-11 recording discovery and media / September 17, 2026 America/New_York.
- **Agent / repository / branch / baseline:** Codex; `vantage-main-server`, `sales-intelligence`, baseline `b92e7458`. Working tree was clean before claim. All changes remain uncommitted for review. No Admin files, branch switch, commit, push or deployment.
- **Status:** ready for review after independent code review and resolution of every finding. [Review](INDEPENDENT-REVIEW.md).
- **Concrete behavior delivered:** registered `recording_discovery` and `media_fetch` workers reuse existing durable claims, dataset scope, transactions, audit and queue wake-ups. Discovery follows merge tombstones, reloads current interaction evidence, preserves account identity/direction/time/Contact Number, links a single event-applicable valid Lead or retains number-level ambiguity, and updates recordings[].lead_conversation_id atomically with media intent. Pending IDs never fabricate conversations. Eligibility records policy, decision, scope and missing CSI-05/10 inputs without duration, voicemail, sampling or closed-work gates. Media reads shared-client metadata then bounded streaming content; computes SHA-256, validates type/signature and bytes, uploads private immutable account/recording/digest media without overwrite. Explicit retry/permission/throttle/no-recording/failure states, exhausted-claim recovery and current eligibility at completion. Owner Coverage reports stored capability and recording counts without writes. Default queue/recovery wiring and five-minute authenticated cron are tested.
- **Files owned and changed:** exact [FILES.md](FILES.md) manifest; claim and shared-edit addendum in [LEDGER](../../LEDGER.md). New services under `salesIntelligence/conversations`, streaming adapter, recording provider adapter and tests; additive model/config/job/DTO/registration/Coverage changes; existing CSI-04 Coverage test extended; service card/catalog/spec/contracts/evidence updated. Existing seed media helper, qualification and official Lead write paths remain unchanged.
- **Contract/version changes:** [csi-media-v1 imports and obligations](../../CONTRACTS.md#csi-11-concrete-imports-server-relative-september-17). Additive `media_fetch` stage; `failCsiJob` transactional projection and timed deferral; nullable eligibility with status/missing_inputs/scope; Internal conversation direction; interaction discovery projection; optional recording counters in shared Coverage. This written packet is the handoff to CSI-12, Teams C/D/E/F; no external messages sent.
- **Checks actually run:** typecheck exit 0; focused 108/108; CSI-11 replica 19/19; foundation/capture/fan-out/numbers/reads replicas 15/12/11/7/10; qualification 104 pass and 3 pre-existing skips; final provider adapter 2/2; diff check. Exact commands/artifacts and evidence limits: [CHECKS.md](CHECKS.md).
- **Race/idempotency/failure proof:** real disposable Mongo proves replay, uniqueness, transaction rollback, canonical resolution, delayed discovery, concurrency and stale-epoch fencing. Fake providers plus Mongo prove retry outcomes, denied/throttled coverage, eight attempts including process-crash recovery, immutable media, current Owner exclusion, and read-only Coverage. These are implementation proofs, never live capability proof.
- **Known gaps / capability blockers:** CSI-05 and CSI-10 producers remain downstream. Missing evidence stays undetermined and media waits; Team C must enqueue a new revision-specific discovery job when completed original discovery needs refreshed attribution. Live content permission, MIME shapes, private Blob binding, cron/queue and deployed timeout remain Team F G6. Recording content was denied on September 14. No current grant is claimed. Full Coverage settings/AI health and Owner media actions remain downstream. Orphaned immutable Blob uploads after crashes need retention reconciliation; no evidence is overwritten to clean them up.
- **Deployment/config/migration/rollback:** no deployment action or production migration. `SALES_INTELLIGENCE_MEDIA_ENABLED` remains off by default; `SALES_INTELLIGENCE_MEDIA_MAX_BYTES` defaults 26214400. Reuses existing private `BLOB_STORE_ID`/`BLOB_READ_WRITE_TOKEN`, shared RingCentral credentials, `CRON_SECRET` and CSI deployment/database binding. No new secret/store or Redis. Existing CSI-01 account-attribution/index migration is prerequisite; this patch needs no new data/index migration. Roll back by keeping media flag off and reverting code while preserving additive state, jobs and immutable evidence; do not recreate the obsolete global recording index or overwrite seed media.
- **Next dependency:** CSI-12 consumes `LeadConversation.state=media_stored`, `media_digest_sha256`, private media metadata, current analysis_eligibility and the `pending_stage:transcription` hook (`MEDIA_STORED_NEXT_STAGE`). It owns durable STT scheduling, budget admission, redaction and transcript versions. CSI-11 enqueues no transcription or analysis job. D preserves number-only scope under ambiguity; E renders stored availability without implying live grants; F independently re-verifies capability at G6.
- **Ledger updated:** CSI-11 → review (findings resolved), dated integration note and exact ownership. Other issue readiness is unchanged.

## Independent review

September 18 follow-up: at the Owner's request a fresh reviewer (`/root/csi11_final_review`) reviewed the full patch and identified two further gaps: rescheduling after a completed eligibility exclusion and bounding shared token waits. Both are fixed and regression-tested; reviewer reports no remaining actionable findings. Expanded replica: 20/20. Qualification/provider/media checks: 111 passed, 3 pre-existing skips. [Final verification](CHECKS.md#september-18-fresh-review-verification). Media rescheduling semantics are updated in csi-media-v1; no new migration or configuration is needed. Patch remains uncommitted with flags off.

Initial reviewer requested four corrections: event-scoped attachment eligibility, killed-eighth-claim failure projection, completion-time eligibility recheck, and seed-media capability honesty. All resolved with replica regressions. Follow-up recommendations (exact-session test, default recovery wiring test, sanitized operational events) also closed. Final separate-agent signoff: **no remaining actionable findings**; ready for patch review. Reviewer independently executed 17/17 replica cases and reviewed the final expanded 19/19 artifact. [Finding-by-finding report](INDEPENDENT-REVIEW.md).
