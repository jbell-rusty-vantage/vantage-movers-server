# Shared contracts and integration handoffs

## CSI-16 certification restamp

CSI-16 certification packet (September 19 Owner session): fresh local evidence and exact non-green results are in [HANDOFF](evidence/csi-16/HANDOFF.md), [CHECKS](evidence/csi-16/CHECKS.md), [ACCEPTANCE](ACCEPTANCE.md), and [G6](evidence/csi-16/G6.md). G4 has a media Retry-After failure; G5 is partial with a generic replay-label integration failure; current production capabilities are not probed. Owner full rollout follows in AFTER-16 D+E; no cutover occurred here.

## CSI-15 local implementation contract (September 19)

CSI-15 landed on local `main` at `561e048960cd914f37a337addada8b459b5296f1`; original final validation tracked in [CSI-15 checks](evidence/csi-15/CHECKS.md). This section supersedes the old non-executable Coverage backfill notice. No MCP schema/tool change or production authorization.

| Consumer | Concrete interface | Contract |
| --- | --- | --- |
| Owner / Admin | POST `/api/v1/admin/sales-intelligence/backfill`; `backfill/plan.ts:commandPlanBackfill` | Existing strict `plan_backfill` command, signed Owner, production scope and Idempotency-Key. Response reports configured days, requested bounds, actual bounded window bounds and windows planned. Day count zero creates no windows. Same key/payload replays; changed payload or conflicting same-start window bounds fail. A new command can resume failed/permission-paused windows from the saved page. |
| Capture / cron | `backfill/step.ts:runBackfillStepOnce`; `numberActivity/reconcileCallLog.ts:projectCallLogBackfillPage` | Oldest eligible daily window, one provider page, separate global backfill/window fences. Checkpoint, complete marker, monotonic watermark and activation intent commit together. Partial page replays through canonical capture idempotency. Retry-After and permission pauses retain page/failure allowance. |
| Outreach / queue | `backfill/worker.ts:runBackfillActivationJob`, `drainBackfillActivationJobs`; `backfill/readiness.ts` | Activation waits for later planned capture and paged attachment work, then processes ten known calls newest first. Successful continuations return the job to pending without consuming a failure. Mongo repair recreates missing activation wake-ups. Existing Outreach fulfillment and official closure remain authoritative. |
| Discovery / STT / analysis | `CSI_LIVE_JOB_PRIORITY=0`, `CSI_BACKFILL_JOB_PRIORITY=-100`; `jobs.ts:claimCsiJob` | Historical provenance carries lower priority through existing stages; live admission also protects explicit historical job-id claims. Historical analysis stores mode `backfill`. Budget/provider pause recovery uses the existing job and saved stage. |
| Budget period / recovery | `budgetPeriod.ts:ensureCurrentCsiBudgetPeriod`; transcription and intelligence drains | Enabled drains activate the current policy-timezone calendar month once, preserving covering custom bounds and existing spend/cap. Overlapping active periods fail explicitly. Cap increases and available current-period allowance resume the saved stage. An expired eighth intelligence claim without a valid submitted receipt becomes dead-letter/failed; an existing receipt is recovered without a model call. |
| Retention / evidence | `retention.ts:runRetentionOnce`, `retentionPolicy.ts:resolveRetentionDays` | Persisted policy wins; engineering defaults are audio 90, redacted content 365, activity 730 days. Separate retention lease, privileged fenced raw writes, content tombstones and purge-aware reads/submission/application prevent copying removed evidence. Original-context rerun rejects missing retained evidence. Purge-during-upload records a private `result.pending_blob_delete` on the completed media job; immediate deletion is best effort and daily retention retries the bounded cleanup queue, clearing only after deletion succeeds. Synthetic race proof is recorded in CHECKS; production remains uncertified. |
| Admin Coverage | `dto.ts:ownerCoverageDtoSchema`, `ownerCoverage.ts:readOwnerCoverage`, `backfill/coverage.ts:readBackfillCoverage` | `backfill={available:boolean,owner_triggered:true,days:number,planned:number|null,partial:number|null,complete:number|null,failed:number|null,known_complete_through:string|null,gaps:{from,to,reason}[],note:string}`. Running windows contribute to partial. No stored windows means null counts. Counts cover all stored windows; gaps list covers the earliest 500 windows and discloses truncation. Watermark is the monotonic captured upper bound, not a claim of continuous coverage or completed Outreach activation. Note reports pending activation. |

Admin consumes only this Coverage addition in its schema/view and focused fixture tests. The view displays server facts and reasons; it adds no planning button or client policy engine. Existing SSE refetch behavior remains unchanged. Cron registration adds backfill every 15 minutes (day count gated) and retention at 04:30 (disabled master flag is a no-op); queue/recovery also consumes durable activation work. No deployment flags were enabled.

## September 19 execution clarification

[SPRINT-PLAN](SPRINT-PLAN.md) records current status; dated implementation sections below retain their original evidence scope. CSI-13 now consumes application jobs. CSI-18 server depends on CSI-06/13/17; its UI additionally depends on CSI-08. The received design export is presentation input, not a DTO freeze. Team E must reconcile actual routes/schemas and publish an intake matrix before binding it; CSI proxy Idempotency-Key forwarding and live BFF are now implemented and freshly exercised in CSI-16; the following historical sections retain their original dates. No wire contract changes are made by this planning revision.

## CSI-13 runtime/application contract (September 19)

Consumes the existing CSI-01/06/10/12/17 contracts. No MCP schema/tool changes. [Service](../../knowledge/services/sales-intelligence-analysis.md), [handoff](evidence/csi-13/HANDOFF.md), [checks](evidence/csi-13/CHECKS.md). CSI-18 remains separate.

| Consumer | Exact server-relative import / DTO | Contract |
| --- | --- | --- |
| Queue/cron/F | `src/services/salesIntelligence/analysis/worker.ts`: `runIntelligenceJob(jobId?, stage="analysis", deps?)`, `drainIntelligenceJobs(deps?)` | Existing `analysis` and `number_refresh` stages only. Analysis refs remain `[conversation_id,transcript_snapshot_id]`. Current eligibility/version before provider; no STT. Recover same run's receipt before invocation. |
| Runtime | `analysis/runtime.ts`: `invokeIntelligenceAgent(InvocationInput)`, `RuntimeLimits`, `InvocationStep` | AI SDK ToolLoopAgent, Gateway, authenticated remote HTTP MCP twelve-tool surface, stored prompt/schema retrieval and verification. Closed budgets, captured coverage pages, single repair, receipt-only success. Dependency injection accepts an SDK LanguageModel for local proof; capture/intake are real. |
| Application/C | `analysis/apply.ts`: `runIntelligenceApplicationJob(jobId?, deps?)`, `drainIntelligenceApplications()` | Existing application refs `[run_id,submission_id]`; five findings per transaction under `checkpointCsiJob`. Per-effect outcomes call CSI-06. Cursor and effects commit together; final publication is a separate fenced transaction after all batches. |
| Submission/recovery | `analysis/readiness.ts`: `resumeApplicationIntents()` | New submitted intents pause `invocation_pending` until orchestration marks `invocation_complete`. Disabled consumer uses `consumer_unavailable`. Resume only compatible receipts and explicitly identified application-disabled pauses, not arbitrary permission/budget reasons. |
| Source changes/F | `analysis/scheduling.ts`: `scheduleNumberIntelligence(numberId,session)`, `scanIntelligenceChanges()` | Existing SyncState cursor/lease scans five numbers. ContactNumber `intelligence_schedule={fingerprint,generation,job_id}` coalesces meaningful changes; original Outreach refresh jobs retain their subjects. Clock/updatedAt changes alone cannot invoke a model. |
| Authority/C | `outreach/effects.ts:applyOutreachEffect` | Conversation run adapter checks persisted Outreach pointer, exact source conversation and current attached event identity. Candidate/foreign subjects denied; same number does not authorize Lead effects. Existing CSI-06 effect decisions remain authoritative. |
| Read DTO/E | Existing `LeadConversation.summary/latest_completed_run_id` and `ContactNumber.running_summary` | Publish only after application boundary; old workers cannot replace newer current analysis. Run status/counts distinguish completed versus stale and applied/blocked/review. Source citations remain immutable; exact locator/entailment are `not_run`. |
| Budget/A/F | Existing reservation/reconciliation, additive reservation usage fields | Persist model/pricing/input/output/reasoning/observed cost. Missing billing is unresolved/null, with reservation retained. No policy/budget initialization. Bounded prices/limits must fit existing per-recording/monthly admission policy. |

Registration reuses existing queue/bootstrap and cron router; extract/apply paths run every five minutes plus existing recovery. Defaults remain off. No deployment, live model/STT/provider access or production initialization occurred. No CSI-18 correction IDs or controls are introduced. Prior CSI-17 failed checkpoint and CSI-14 P2 remain recorded in their original evidence.

## CSI-17 coordinated implementation contract (September 18)

The operational Job Number timeline read reuses `src/services/jobNumberTimeline/mongo-evidence-loader.ts` with optional `maxRowsPerQuery: 200`. Every `find` cursor has a 201-row overflow sentinel and five-second query limit; overflow aborts with `EVIDENCE_LIMIT_REACHED`, never a truncated evidence snapshot. Existing consumers omit this option and retain default behavior. The MCP subagent owns this narrow coordinated loader seam, `mongo-evidence-loader.test.ts` instrumented real-loader coverage, and its Service documentation in addition to the operational adapter. This correction occurred after the initial quality snapshot.

Team D owns both repositories on `sales-intelligence`. Main server owns `analysis/contracts.ts`: strict tool arguments, closed evidence record/page DTOs, prompt template and generated envelope schema. `scripts/generate-csi-intelligence-contract.ts` produces MCP `lib/intelligence/generated/intelligence-contract-v1.json`; authoritative Zod parsing retains cross-field refinements that JSON Schema does not express. MCP is the sole model-facing tool/prompt/resource provider; every data tool delegates to the existing dedicated internal server boundary.

Owner additions extend the nine original tools with `query_operational_records`, `search_ringcentral_calls`, and `get_ringcentral_call`. The first accepts a closed dataset selector (Form/Call Leads, Job Number timeline, Bookings, Cancellations, Agents, Granot CRM Sources, RingCentral queues/Users), bounded text query and cursor/page. It is a scoped Mongo read facilitator, not the broad endpoint's arbitrary collection/filter/aggregation surface. Provider reads use fixed Call Log endpoints, server-derived account/number scope, fake transport proof and default-off `SALES_INTELLIGENCE_PROVIDER_READS`; no live provider request is authorized by this implementation task.

Wire requests: GET context with no arguments; POST read `{tool,args}`; POST submit `{idempotency_key,envelope}`; GET submission for pure receipt/auth recovery. Both dedicated key and signed run token are mandatory. Every dedicated MCP request revalidates through the status route, requiring the submit permission. Status returns `{run_id,status,submission,prompt_context}`; prompt context contains persisted rendered prompt/version, schema version/digest and mode. No model-selected actor, run, dataset, account, URL or collection is accepted. Parent owns server intake/capture/core reads/models/auth/router; MCP subagent owns MCP transport and server `analysis/operational.ts` plus its focused tests. Existing CSI-10/14 shared edits are preserved; dashboard and CSI-13 application consumer remain untouched.

Concrete CSI-13 imports, all twelve argument/result contracts, exact schema resource, error/cursor/coverage semantics, replay, recovery and paused handoff are in [CSI-17 API-CONTRACT](evidence/csi-17/API-CONTRACT.md). `analysis/run.ts` exports `prepareIntelligenceRun`, `recoverIntelligenceSubmission`, `renderIntelligencePrompt`, `intelligenceSchemaDigest`; `analysis/capture.ts` exports `captureIntelligenceRead`; `analysis/submit.ts` exports `submitIntelligenceAnalysis`, `readIntelligenceSubmission`. MCP is the sole model-facing provider; these are server orchestration seams, not alternative tools. Owner-added record citation kinds are generated from the authoritative envelope schema, with additive digest `18a4746f6343c4b093ef449693b685872e3f912ae36fe87e4d49196fc1ecfed7`. Existing cross-field checks remain. Run fields `schema_digest`, `manifest_snapshot_ids`, `evidence_count`, `evidence_bytes`, submission `manifest_digest`, and job `evidence_fence` are additive; existing unique indexes suffice. No production migration was run.

## CSI-14 Owner destination (September 19)

Product rule now in [01 §3/§9](../01-specification.md), [02 §8/§10](../02-domain-models.md), [03 §8](../03-server-pipeline-and-jobs.md), [04 §5](../04-server-routes.md) and [05 §7](../05-owner-dashboard-ux.md). Preview/send destination is a current User extension on the stored directory snapshot for that RingCentral account (`rc_account_id` + `rc_extension_id`). A reviewed Rep Identity Link is optional metadata, not a send precondition. Rate limit is per User extension. Implemented September 19; [destination evidence](evidence/csi-14-destination/HANDOFF.md). This note does not authorize live send, CSI-15, or inventing Team Messaging person ids.

## CSI-14 coordinated additions (claimed September 18)


Team C owns `services/salesIntelligence/nudges/**`, its synthetic tests and evidence. Additive shared changes: OwnerRepNudge durable submission metadata; nudge validation, DTOs, channel configuration; Owner routes and authenticated repair cron; repair-only job dispatch; Outreach detail history; audit invalidation kind. Existing CSI-06/10 behavior and current-source refinements are preserved. Team E owns dialogs; CSI-08/09 consume browser contracts, not an implemented browser integration. CSI-13 receives no send capability. No live provider proof is authorized.

Concrete imports and synthetic browser examples are published in [CSI-14 API contract](evidence/csi-14/API-CONTRACT.md). Import `previewNudge`, `sendNudge`, `NudgePreviewDto`, `NudgeSendDto` from `src/services/salesIntelligence/nudges/commands.ts`; templates from `nudges/templates.ts`; injectable `NudgeAdapter` / `createNudgeAdapter` from `nudges/adapters.ts`; `NudgeDto`, `nudgeDtoSchema`, `listNudges`, `nudgeHistoryPage` from `nudges/reads.ts`; `runNudgeRepairJob`, `recoverNudgeRepairJobs`, `drainNudgeRepairJobs` from `nudges/repair.ts`. All abbreviated paths start at `src/services/salesIntelligence/`.

CSI-06 detail now returns `data.nudges:{items,next_cursor}`. CSI-10's Owner review input and link DTO accept/expose optional `rc_team_messaging_person_id`; reviewed changes still use the existing successor interval semantics. `resolvePolicy` accepts an optional transaction session for coherent send revalidation. The existing OwnerRepNudge collection/indexes gain durable command/recipient/submission/receipt metadata and revision. Infrastructure adds audit kind `nudge` and job stage `nudge_repair`; only reconciliation is registered with queue/generic recovery. The new authenticated cron is registered at `*/5 * * * *`. No send route/tool is granted to CSI-13, queue principals or model runs.

Consumer handoff: Team E/CSI-08/09 implement dialogs against preview/send/history; CSI-06 reads the history page and audit subject; CSI-10 remains identity authority; deployment/operations own separately approved current grants/configuration/live proof. Pending/unknown delivery must never trigger automatic resend/fallback. All persisted states reserve the link's rolling-hour limit; default six. Dashboard remains untouched.

The existing `outreach/timeline.ts` read adapter now includes `nudge` invalidations as the already-defined `nudge` timeline kind. Subject lookup includes Outreach primary-number records even without a separate attachment edge. This is read-only composition; lifecycle events never imply completed customer work.

## CSI-10 production seed (September 19)

Additive GET DTO field `rc_direct_numbers` on `repLinkDtoSchema` / `toRepLinkDto`. It is the snapshot DID list already stored on the link at propose/create/review. Admin review uses that list to set `nudge_channels_allowed`: `pager` always; `sms_to_rep` only when the array length is exactly one. Team Messaging person ids stay omitted. Propose still writes empty channels. This is not a send grant.

Live seed used existing Owner routes only. Snapshot `6aaea089984b82cb35a76522` on account `62948571023`. Nine unique first-token Users were proposed and reviewed. Joshua L / Roy W stayed unmatched because Agents `Josh` and `Roys` have empty aliases. Evidence: [csi-10-seed handoff](evidence/csi-10-seed/HANDOFF.md).

## CSI-10 coordinated additions (September 18)

Read DTO exports also include `repProposalDtoSchema`, `repDirectoryEvidenceSchema`, `repListDtoSchema`, `repDetailDtoSchema`. GET consumes persisted `propose_reps` command results; Users without current-snapshot proposal evidence are `not_proposed`. Matching occurs only on POST, never GET. Proposal evidence remains in the existing durable command response, including ambiguous/unmatched results, without a second identity collection.

Server baseline `3355c6d`, `sales-intelligence`; dashboard untouched. Reuse RepIdentityLink and its current unique index. Extension writes serialize through an account/extension-specific existing SyncState row; historical interval overlaps are checked in that transaction. No parallel collection or command framework. Additive job stage `rep_identity_reevaluate` stores a typed `rep_identity_window` (account, extension, ISO bounds, change id, temporal cursor); `input_refs` remains ObjectIds. Default queue dispatch and minute job-recovery register the worker under ENABLED. Existing stage flags still govern Outreach/media/STT execution.

`repIdentity/resolve.ts`: `resolveRepIdentityAt`, `resolveRepIdentities` use [effective_from,effective_to), retain reviewed retired history, and expose unknown/proposed/conflicting/excluded-role states plus evidence fingerprints. CSI-06 and CSI-11/12 consume this resolver; CSI-08/09/13 and future CSI-14 must use it rather than status-only queries. A roster identity is never actual-attempt, human-contact or promising-speaker evidence.

`repIdentity/propose.ts`: `proposeRepCandidates`, `loadRepDirectory`; stored account-scoped User extensions only, all name/alias/first-token matches remain proposals. Ambiguous/unmatched Users are directory DTO evidence, not fabricated required Agent links. `commands.ts`: `createRepLink`, `proposeRepLinks`, `reviewRepLink` use trusted Owner, strict CSI schemas, revisions, command replay and transactional audit. Reviewed edits create successors and preserve historical role/Agent/channel metadata. `reads.ts`: `repLinkDtoSchema`, `toRepLinkDto`, `listRepLinks`, `readRepLink`, `repListQuerySchema`; metrics remain explicitly unknown. `scheduling.ts`: `scheduleRepIdentityReevaluation`; `worker.ts`: `runRepIdentityReevaluationJob`, `drainRepIdentityReevaluationJobs`. These are server imports, not messaging authorization. CSI-14 remains not started.

Status: CSI-01 complete; **G1 foundation contracts frozen** after independent GPT-6 review and resolution of all five substantive findings. Runtime contracts below are the Step 2 handoff. B–E feature services remain unimplemented. Review packet: [STEP2-HANDOFF.md](evidence/csi-01/STEP2-HANDOFF.md). Grok's original handoff remains preserved.

| Contract | Authority | Producer → consumer | Required handoff |
| --- | --- | --- | --- |
| Call identity and evidence | 02 §§1–3; 03 capture | B → C/D/E | Canonical id, projection revision, event/observed time, external number, participants, recording/coverage; duplicate and transfer fixtures. **CSI-02 implemented:** see [CSI-02 concrete imports](#csi-02-concrete-imports-server-relative-september-17). |
| Attachment attribution | 01 §5.2/§6 | C → B/D/E | Exact/Likely/Unsure, scoped subject, blocked reason; no model-selected Lead authority. |
| Transcript version | 02 §6; 10 evidence | B → D/E | Immutable redacted segments/digest, speaker evidence/unknown, complete coverage and media state. |
| Outreach/action commands | 01 §§5–8; 04 | C → D/E | Multiple nullable-due actions, assignments, Owner instruction scopes/revisions, clocks/review/restriction behavior. |
| Envelope schema | 10 §3 | A/D → all | Implemented and frozen: `src/validation/intelligence/intelligenceEnvelope.validation.ts` (`csi-envelope-v1`). Fixtures: `src/validation/intelligence/fixtures.ts`. Schema validation only — not evidence authorization or effect application. |
| Scoped MCP protocol | 10 §2; 04 §6 | D → agent worker | Tool allowlist, credential claims, run subject enforcement, paginated read schemas and snapshot capture. |
| Submission and effects | 10 §§5–8 | D + C → E/F | Idempotent receipt, stable obligation identity, application/review status separation, CAS failure and partial outcomes. |
| Owner interventions | 10 §7; 04 §4 | C/D → E | Immediate correction before reanalysis, original/current mode, exact confirmation target, versioned assessment. |
| Policy and derivation | 01 §8 | A/C → all | Versioned settings, effective schedule, stamped default deadlines, pure derive DTO and clock fixtures. |
| Jobs and audit | 02 §15; 03 §14 | A → B/C/D/F | Dedupe/lease epoch/recovery, submit→apply atomic scheduling, immutable history and evidence retention. |
| Owner DTOs/live | 04; 05 | B/C/D → E | Stable subject keys, one Attention row/all reasons, full action list, review-only closed rows, invalidation types. |

## CSI-06 concrete imports (server-relative, September 18)

Contract `csi-outreach-v1`; server `sales-intelligence`, baseline `999c63d`; dashboard untouched. [Service](../../knowledge/services/sales-intelligence-outreach.md), [handoff](evidence/csi-06/HANDOFF.md). CSI-10/14 are not implemented.

| Consumer | Concrete interface | Obligation |
| --- | --- | --- |
| D / CSI-13 | `outreach/effects.ts`: `outreachEffectInputSchema`, `planOutreachEffect`, `applyOutreachEffect(input,context)` | Validate finding/snapshot evidence and run authorization in D first. Supply trusted transaction context under the application's job lease fence. C reloads live state, owns effects, stable source matching, chronology, Owner field precedence and effect receipts. Refresh displayed revisions before each independent effect. Same receipt replays; changed payload conflicts. Do not emit state/rank/DB paths or duplicate these rules in D. |
| D / CSI-13 | `outreach/numberReview.ts`: `ensureNumberReview`; `review/restrictions.ts`: `applySpokenRestriction`; `review/items.ts`: `openReview` | Transactional primitives for eligible number-only findings. Use current canonical interaction identity and validated run-scoped evidence. Number Review eligibility never evades ambiguous/closed Leads. Restrictions share one implementation with `applyOutreachEffect`; D retains its finding/effect publication transaction. No model-selected Owner actor or permanent suppression. |
| D / CSI-18, E / CSI-08 | `followups/commands.ts`: `commandOutreach`, `applyOwnerCommandInTransaction`, `OUTREACH_COMMANDS` | Immediate Owner operations, even while AI is offline. Use the exported transaction primitive inside D's correction/retract command, avoiding nested command transactions. Single action id/revision; overall assignment is separate. Notes cannot imply human contact. Owner routes require ENABLED and OUTREACH_ENSURE. |
| E / CSI-08/09 | `outreach/reads.ts`: `toOutreachDto`, `readOutreach`, `readOutreachByLead`, `readNumberOutreach`, `listReviewItems`; shared `dto.ts` schemas | GET is read-only. Number detail returns every connected Outreach Record in `outreach_records[]`. Preserve nullable deadlines, separate promised/action/overall responsibility, all reasons and blockers. No client clock/state engine. Raw Owner instructions in detail provide context for D's review UI; later run/assessment/nudge sections remain their respective teams' work. |
| E / CSI-08, CSI-07 | `outreach/derive.ts`: `derive`, `attentionDue`; `outreach/attention.ts`: `readAttention`, `attentionQuerySchema`, `publishAttentionSnapshot` | All open actions participate. One highest-priority row per subject, all reasons. Contractual overdue survives snooze but ranking does not. Worker creates immutable snapshots; GET binds cursor/as-of/filter without writing. Pending projection has a nullable count. Closed work is review-only. CSI audit invalidations include action/record/restriction/clock changes. |
| CSI-07/09/13 | `outreach/staffing.ts`: `addStaffedMinutes`, `staffedMinutesBetween`, `resolveActionDate`, `resolveActionDateText`; `outreach/store.ts`: `vantageEventFingerprint` | Eastern defaults 30/15/1440; policy-stamped promises; day-only wait next opening; explicit times outside shifts; ambiguous wording/DST stays undated. Fingerprints contain meaningful official/action facts, not observed timestamps. Calendar changes never rewrite stamped due dates. |
| Queue / recovery | `outreach/worker.ts`: `runOutreachEnsureJob`, `drainOutreachEnsureJobs`, `runOutreachEnsureOnce` | Registered stage-specific claim and final lease fence. Queue `{job_id}` only. Applied-at source scan, bounded baseline/repair sweeps, minute cron, recovery, clock events and Attention publication. No provider calls. |

Coordinated additions: immutable nullable `source_interaction_id` in Followup/Restriction schemas and `source_due_at` on Followup; optional shared DTO fields and explicit pending Attention state; default queue/cron/admin registration, package script and Vercel schedule. CSI-05 hook consumes Outreach reactions and adds revision-specific Outreach fan-out without replacing suggestion/command logic. CSI-02 contact-type merge preserves an Owner correction to `unknown` as well as voicemail/human. CSI-03/04 replica expectations now recognize the registered disabled worker and populated plural DTOs. Existing unique indexes/TEST_MODE fences remain required; no production migration or flag changes.

`outreach/timeline.ts: outreachTimelineSource` is registered with Number Activity's existing keyset merge. It exposes append-only Owner notes, assignments, follow-ups and review/restriction history. `appendCsiAudit` accepts optional `happened_at` for source chronology while retaining `recorded_at` as observation time. Independently dated commitments from one conversation remain distinct; when several simple callbacks could match the same later attempt, C opens completion-target review instead of completing all of them. Effect receipts return the actual follow-up, restriction, interaction or explicit review target.

## CSI-05 concrete imports (server-relative, September 18)

Contract `csi-attachment-v1`; CSI-05 only. Server `sales-intelligence`, baseline `c73df86`; dashboard untouched. [Service](../../knowledge/services/sales-intelligence-attachment.md), [handoff](evidence/csi-05/HANDOFF.md).

| Consumer | Concrete interface | Obligation |
| --- | --- | --- |
| D / CSI-06 / CSI-11 | `salesIntelligence/attachment/suggest.ts`: `resolveAtInteraction(edges, interaction)`, `AttachmentAttribution`, `leadWindow`, `suggest`, `ambiguityFanIn`; `store.ts`: `attachmentPolicyInput` | Resolve EACH event by time and exact provider identity. `lead_effects_allowed:false` means no Lead-specific effect. `lead_ref:null` preserves number-level context; blocker distinguishes unlinked, ambiguous and competing Attached. Never let the model select a Lead or treat lifetime edge counts as identity. |
| E | `attachment/reads.ts`: `listAttachments`, `attachmentListQuerySchema`, `attachmentDtoSchema`, `toAttachmentDto`, `AttachmentDto`; shared `dto.ts` schema/type re-exports | GET `/attachments?contact_number_id=...` or `?lead_model=FormLead&lead_id=...`; `_id` cursor, default 50/max 200. Exact / Likely / Unsure / Confirmed by you; rejected certainty label null with Rejected state. Evidence dates, history and cached Lead snapshot are read-only. |
| E / Owner boundary | `attachment/commands.ts`: `commandAttachment` | Existing CSI-01 discriminators: `attach_lead`, `reject_attachment`, `detach_attachment`; routes `/attachments/attach`, `/attachments/:id/reject`, `/attachments/:id/detach`. Reason is Owner evidence, Idempotency-Key mandatory. Existing pair expected_revision is edge revision; absent pair expected_revision is displayed Contact Number revision. Optional expected_revisions accepts only that number/edge. Conflict requires refetch. Both ENABLED and ATTACHMENT_REFRESH must be on. |
| B / F | `attachment/refresh.ts`: `runAttachmentRefreshJob`, `drainAttachmentRefreshJobs`, `runAttachmentRefreshOnce` | Registered `attachment_refresh` handler, five-minute `/api/cron/sales-intelligence-attachment-refresh`, minute extraRecovery. Existing B `number:<id>` jobs keep `input_refs=[interactionId]`; current canonical interaction resolves number. Internal lead/number-scan/change intents are same-stage, bounded and durable. No raw queue state. |
| CSI-06 | `attachment/hooks.ts`: `AttachmentChanged`, `onAttachmentChanged(change, session)` | Transactional `{number_id, revision}` intent. CSI-05 only registers rediscovery fan-out. CSI-06 owns future identity-review/Outreach reactions; no Outreach worker, state, clock or ensure implementation delivered. |
| CSI-11 | `hooks.ts`: `rediscoverAttachmentPage`; `conversations/eligibility.ts` now consumes CSI-05 resolver | New `csi:recording_discovery:interaction:<id>:attachment:<number>:<revision>` jobs, same interaction input ref, never reopen a completed original discovery job. Page 500 with durable continuation. No inferred reviewed rep mapping. |

Shared-file coordination: additive nullable `NumberLeadAttachment.evidence` fields `interaction_id`, `provider_account_id`, `identity_kind`, `identity_value` pin exact evidence; additive `SyncState.cursor.attachment_source_id` supports timestamp ties without abusing EntityChange fields. No new indexes/config flags or v1 router edits; existing Owner/boundary routers receive additive registration. Shared edits include cron, jobDispatch, vercel.json, package.json, DTO type exports and CSI-11 eligibility adapter. No other team runtime is replaced. `EntityChange.applied_at` remains a later scan source, never a CSI origin sink. Existing `contactNumbers.ts` has no rebuildSearchTerms export in this baseline; attachment projection updates reuse the same bounded search-term rules in `attachment/store.ts` without changing capture rollups or writing Outreach.

## CSI-12 concrete imports (server-relative, September 18)

Contract `csi-transcript-v1`, built on `csi-media-v1`. [Service](../../knowledge/services/sales-intelligence-transcription.md), [handoff](evidence/csi-12/HANDOFF.md), [checks](evidence/csi-12/CHECKS.md). Flags remain off; no production capability is asserted.

| Consumer | Concrete interface | Obligation |
| --- | --- | --- |
| Queue / F | `salesIntelligence/conversations/transcribe.ts`: `runTranscriptionJob(jobId?, deps)`, `drainTranscriptionJobs(deps?)`, `recoverExhaustedTranscriptionJob(jobId?)` | Stage `transcription`; queue, five-minute `/api/cron/sales-intelligence-transcribe` and default minute recovery share the worker under `STT_ENABLED`. One bounded recording STT call per invocation; at most five hook jobs scheduled. Eight transient claims; budget/permission/eligibility waits do not burn attempts. |
| CSI-11 / F | `salesIntelligence/conversations/transcriptionScheduling.ts`: `scheduleTranscriptionJobs(max=5, publish?)` | Rechecks authoritative eligibility for at most five due unmarked stored-media rows, including cached undetermined/excluded inputs. Unresolved rows defer ten minutes to avoid starvation. Eligible media atomically creates a job plus `transcription_job_digest`. Dedupe key is `csi:transcription:conversation:<id>:<media-sha256>`; `input_refs=[conversation ObjectId]`. Pre-STT exclusion completes and refunds that nonprovider claim; only completed `result.reason:excluded` may reopen after eligibility restoration, keeping prior attempts. Successful/failed jobs and changed model/analysis retry never receive fresh STT allowance. |
| D / CSI-13 | `IntelligenceEvidenceSnapshot` source_type `transcript`; `transcript_version=csi-transcript-v1:<media-sha256>` | Immutable `(conversation_id,transcript_version)`. Redacted `response.transcript`, ISO created_at, media digest, reservation id, pricing and actual integer cents or explicit null. `segments` are `{sid:1..n,start_ms:number|null,end_ms:number|null,timing_source:provider|unavailable,speaker:rep|customer|unknown,text}`. Preserve timing only where provider-supplied; Gateway roles remain unknown. Evidence refs use `{source:transcript,snapshot_id,conversation_id,transcript_version,segment_ids,quote}`. |
| D / CSI-13 | Durable `analysis` job, `input_refs=[conversation ObjectId,snapshot ObjectId]`; dedupe `csi:analysis:conversation:<id>:<transcript-version>` | Read the pinned snapshot, not a mutable latest cache. Completion atomically enqueues this job if currently eligible. Excluded completion suppresses it; undetermined completion keeps a paused `eligibility_pending` job. `resumeTranscriptAnalysisJobs()` rechecks at most five waits/excluded intents in recovery, pinned to snapshot version and media digest. Later exclusion closes the intent and clears pending analysis; restored eligibility reopens only that excluded current-version intent. Stale intents cannot modify newer conversation state. Empty STT is a terminal billed `empty_transcription` result with no snapshot/analysis or paid retry. Team D must still recheck current eligibility and staleness before invoking its worker; CSI-12 implements no analysis/extract/contact-type or application worker. |
| B / F | `conversations/transcriptionProvider.ts`: `BlobAudioReader`, `SttProvider`, `SttResult`, `gatewaySttProvider`, `readStoredAudio` | Immutable private Blob path only; bounded size/digest verification. Configurable model via `csiProviderConfiguration().transcriptionModel`. Raw STT text/SDK response remains in memory, redacted before persistence; only bounded error codes escape. No required timing/diarization fields. |
| All budget callers | `reserveCsiBudget({kind:stt,...})`; `reconcileCsiBudget(id,actual,release?,session?)` | Optional kind is compatible with existing callers. Existing transaction session allows atomic evidence/cost/job commit; never pass a session across a network call. Duration × explicit positive `SALES_INTELLIGENCE_STT_CENTS_PER_SECOND` reserves rounded-up integer cents. Actual derives only from reported Gateway cost, rounded up to the shared integer-cent unit. Unknown outcome retains reservation; missing reported cost preserves transcript with nullable cost and `stt_cost_unreported`, never reruns successful STT to recover billing. |
| E / reads | `LeadConversation.transcript_segments`, `latest_transcript_version`, cached transcript and `cost_cents.stt` | Existing seed media/cache preserved. `stt` can be null for unreported billing; never render it as known zero. No Admin changes delivered by CSI-12. |

Foundation integration: no new indexes/migration. CSI-01 prerequisites still apply. Owner increases to either monthly or per-recording caps resume paused budget jobs; period initialization already resumes them. No local key or model catalog presence grants production permission. Team F owns live compatibility, pricing/billing verification and reconciliation of ambiguous outstanding reservations. Process death after provider success but before Mongo commit can repeat a network call; committed evidence never repeats. Explicit transcript regeneration commands are not implemented here; automatic version creation requires changed media.

## CSI-11 concrete imports (server-relative, September 17)

Contract `csi-media-v1`, built directly on `csi-foundation-v1`, `csi-capture-v1`, `csi-fanout-v1` and `csi-numbers-v1`. [Handoff](evidence/csi-11/HANDOFF.md), [checks](evidence/csi-11/CHECKS.md), [independent review](evidence/csi-11/INDEPENDENT-REVIEW.md). No live recording grant is asserted.

| Consumer | Interface | Obligation |
| --- | --- | --- |
| CSI-12 / D | `src/models/LeadConversation.ts`: existing account-scoped recording identity, `media`, `media_digest_sha256`, `analysis_eligibility`, nullable `lead_ref`, `call_interaction_id` | Consume only stored immutable media with current eligibility; no provider contentUri. `pending_stage: transcription` is a handoff hook, not an enqueued STT job. CSI-12 owns scheduling, budget, redaction and versioned transcripts. Preserve completed media/seed evidence and account uniqueness. |
| CSI-12 / C / D | `src/services/salesIntelligence/conversations/eligibility.ts`: `EligibilityInputs`, `decideAnalysisEligibility`, `loadEligibilityInputs` | `eligible:null/status:undetermined` is not exclusion. `missing_inputs` names CSI-05 event-applicable attachment context, missing referenced Lead, or CSI-10 reviewed rep mapping. Only evidence windows containing call time or matching exact Call Lead provider identity apply; Attached outranks Candidate; ambiguity is number-only. Official closed flags and voicemail/duration never exclude analysis. Owner exception hook requires a trusted Owner actor plus audit reference; caller must persist that audit atomically. No Owner UI/process endpoint is implemented. |
| C (CSI-05/10) | `discover.ts`: `runRecordingDiscoveryJob(jobId?, deps)`, `drainRecordingDiscoveryJobs(max?, deps)` | Changes to attachments/rep inputs must create a new revision-specific discovery job (same input interaction ref), not replay a completed original job. Media intents dedupe by conversation and media revision. Rediscovery reuses the latest intent except that a completed `result.reason:excluded` skip advances the media revision once when eligibility returns; concurrent rediscovery dedupes that successor. Active waits and terminal provider failures retain their original attempt policy. Undetermined jobs reconsider inputs every ten minutes. Excluded discovery creates evidence but no media job. Do not infer a reviewed mapping from proposals or names. |
| F / queue / cron | `media.ts`: `runMediaFetchJob(jobId?, deps)`, `drainMediaFetchJobs()`, `recoverExhaustedMediaJob(jobId?)`, `MEDIA_STORED_NEXT_STAGE`; `recordings.ts`: `RecordingProvider`, `RecordingReadError`, `recordingProvider`, `retryAfterMs` | Stage is `media_fetch` (additive enum; old reserved `media` unchanged). One recording per invocation, own stage claim, no network inside Mongo transaction. Default stage handlers and default extraRecovery register both discovery/media under `MEDIA_ENABLED`. Cron is `/api/cron/sales-intelligence-media-fetch`, `*/5 * * * *`. Media success/failure audit and sanitized operational events are separate from deployment proof. |
| E / F | `readCaptureCoverage()` / `coverageDtoSchema`: optional additive `recordings` object, produced by current reads; Owner GET `/coverage` | Counts: pending_discovery, media_pending, media_stored, no_recording, unavailable, failed, eligibility_undetermined. Missing-ID counts are canonical interactions; media counts are recording conversations. Capability precedence: denied > unavailable/failed > verified account/interaction/digest media > unknown. Seed audio counts availability only. Coverage is stored evidence, not a current provider permission probe; never mutates. Existing Owner search/detail/timeline wrappers also carry the additive field. CSI-09 Owner GET uses `readOwnerCoverage()` / `ownerCoverageDtoSchema` for stages, budget, mapping hygiene, flags/models, settings snapshot and a non-executable backfill notice. |
| B / F | `streamingMedia.ts`: `storeRecordingStream`, `uploadImmutableConversationMedia`, `ImmutableUpload`, `MediaValidationError`; `numberActivity/fixtures.ts`: `syntheticRecordingWav` | Bounded private temporary spool, SHA-256 during stream, verified MIME/signature and extension, private Blob stream with overwrite forbidden. Fake upload seam never proves live Blob privacy, binding or network behavior. Immutable key is `conversations/{account}/{recording}/{digest}.{extension}`. Retention must reconcile orphaned uploads after a crash/lease loss. |
| All workers | Additive `failCsiJob` options `mutation(session,{status,next_attempt_at})`, `resumeAt`; returns `{status,next_attempt_at}` | Same session for projection + final lease fence, rollback on expiry. Existing callers retain their behavior. `recording_pending`, `eligibility_pending`, `throttled` and timed `permission_denied` waits decrement the claim's attempt; other errors keep the eight-failure budget. `resumeAt` is server-calculated, not client payload. All callbacks must be network-free. |

**Coordinated foundation corrections:** prior failure API could not commit a projected conversation failure atomically with the job fence, or resume timed permission waits without burning attempts. CSI-11 extends that API instead of creating a parallel lease queue. The shared claimer's crash-exhaustion sweep remains unchanged; media-specific recovery projects killed eighth-attempt failures transactionally, once. `analysis_eligibility` gains nullable eligible/status/missing_inputs/scope; conversation direction gains Internal; CallInteraction gains nullable recording_discovery projection; shared Coverage gains optional recordings counters. Existing index migration/account-attribution requirements still apply; no new indexes or data migration are required for these additive fields. Replica regressions verify CSI-01–04 compatibility. CSI-04's expected Coverage DTO is extended in its existing no-read-mutation test.

**Engineering timing:** 72h absent-ID window from interaction.first_observed_at; 72h content-404 window from conversation.createdAt; pending delay 1m→1h; permission denial +24h; Retry-After seconds/date or default +10m. Unknown eligibility retries +10m. Eight transient failures dead-letter with conversation failed; oversize/type-invalid media is terminal unavailable. Successful media is never overwritten on replay. Flags remain off. These are implementation policies, not provider availability/retention promises. No Redis, STT, transcript, LLM, analysis enqueue, attachment write or qualification change.

Synthetic fixture contract: `syntheticRecordingWav()` is 48 bytes of generated WAV structure with no voice; combine with `inboundConnectedCallLog(...,{duration:1,legs:[],recording:{id}})` for short-call discovery. Replica tests supply `RecordingProvider`/`ImmutableUpload` fakes for 404→success, exhausted 404, denied/throttled and eight failures. Team F must separately re-verify the September 14 recording denial, fixed endpoint/content types, deployment timeout and private Blob store/token binding at G6.

## Minimum cross-team fixture set

- 20-second Form Lead call with clear Friday promise; full analysis and follow-up despite short duration.
- Voicemail containing a callback request; no human-contact stamp.
- Inbound human conversation on a Form Lead; clears No call yet.
- Alex owns Outreach; Jordan promises a callback; Owner separately assigns the callback to Casey.
- Call Friday + send estimate today + an undated availability check on one record.
- Owner corrects date while a prior-snapshot run is applying; Owner wins with model disagreement visible.
- Same run delivered twice and a later run repeating the same promise; one obligation, full run history.
- Old promise followed by fulfilled callback and Booking, ingested out of order; no revived overdue action.
- Contact restriction conflicts with an Owner callback; plan preserved but calling paused.
- No recording/AI budget exhausted while missed-call and Owner commands continue.

## Interface changes

Changing a field/enum/route requires updating its canonical source document, typed schema, example fixture, consumers and acceptance test in one coordinated handoff. A contract change is not complete because one repo compiles. Never retain an old acceptance-gated API behind a new label.

## Readiness checkpoints

| Gate | Evidence | Current status |
| --- | --- | --- |
| G1 Contracts frozen | Independent GPT-6 review approved; substantive findings resolved; concrete types/migrations/DTO fixtures published for B–E | Complete for foundation. [Independent review](evidence/csi-01/STEP2-INDEPENDENT-REVIEW.md). Downstream consumer integration acceptance remains part of feature delivery. |
| G2 Operational loop | Capture → attribution → clocks/actions → Owner UI without AI | Local components/HTTP and bounded browser proof executed; full scenario limits in CSI-16 ACCEPTANCE. Not deployed proof. |
| G3 Intelligence loop | Transcript → scoped MCP reads → submit → auto-apply → intervention | Fresh runtime 23/23, Owner 6/6 and sibling contract 3/3; real SDK/local MCP with fake model. Paid model/STT unprobed. |
| G4 Resilience | Dedupe/races/history/budget/permission/retry/retention proofs | FAIL overall: CSI-15 and acceptance replicas pass except media Retry-After leaf + parent (18 pass/2 fail). CSI-16 F-01 assigned B/A; fixtures unchanged. |
| G5 Owner walkthrough | Acceptance scenarios with real UI and test backend | PARTIAL: isolated current API/Admin browser artifacts and 14-row execution matrix; not-run/capability-blocked portions explicit. Generic replay label F-02 fails integration. |
| G6 Production capability/rollout | Fresh grants/subscription/credentials/model checks and separate deployment evidence | RECORDED, NOT PROBED: eight current capability rows in CSI-16/G6.md. Full cutover AFTER-16 D+E remains separate. |

## September 17 codebase alignment

[Audit and required adaptations](../11-codebase-alignment-audit.md) are part of this delivery contract. Complete the rows assigned to this team and provide integration evidence; current runtime helpers do not already satisfy the revised contracts. [Design intake](../07-claude-design-brief.md) governs the received design export; the September 19 sprint revision records adaptation and validation still required.

## CSI-01 concrete imports (server-relative, September 17)

Contract version `csi-foundation-v1`; envelope `csi-envelope-v1`; run credential `csi-run-token-v1`; initial policy `csi-policy-v1`. Policy versions thereafter are unique immutable version identifiers, with a revisioned active pointer. These interfaces are frozen for B–E implementation after independent review. Exported schemas use Zod; consumer types are inferred from those schemas rather than duplicated declarations.

| Consumer | Import and exports | Obligation |
| --- | --- | --- |
| All | `src/config/domain/salesIntelligence.ts`: `CSI_CONTRACT_VERSION`, action/stage/tool/error enums, `csiFlag`, `csiDataset` | Flags default off; one deployment/database context. |
| D, C | `src/validation/intelligence/intelligenceEnvelope.validation.ts`: `intelligenceEnvelopeSchema`, `intelligenceFindingSchema`, `parseIntelligenceEnvelope`, `IntelligenceEnvelope` | Strict assertions and evidence shapes only; no authority to execute an action. Synthetic examples in adjacent `fixtures.ts`. |
| C, E | `src/validation/v1/salesIntelligence.ts`: `csiCommandSchema`, `CsiCommand`, `csiFollowupInputSchema`, `csiDateResolutionSchema`, `csiSubjectSchema`, `CsiSubject` | Closed command union; expected revisions; ISO UTC strings; nullable dates are meaningful. Handler performs semantic validation. |
| C, E | Same module: `CSI_OWNER_ACTIONS`, `csiActionAvailabilitySchema`, `csiErrorSchema`, `csiListQuerySchema`, `csiPolicySchema`, `CsiPolicy`, `csiSettingsCommandSchema` | Legal-action DTO describes availability; runtime must revalidate before mutation. |
| B–E | Same module: `csiRepInputSchema`, `csiRepCommandSchema`, `csiNudgeInputSchema`, `csiNudgeCommandSchema`, `csiBackfillCommandSchema`, `csiSubmissionReceiptSchema` | Separate strict schemas for endpoint families; these do not send messages or start backfills. |
| E with B/C/D producers | `src/services/salesIntelligence/dto.ts`: `CoverageDto`, `AssignmentDto`, `DerivedDto`, `FollowupDto`, `FindingDto`, `OutreachDto`, `ActionAvailabilityDto`, `AttentionRowDto`, `ReviewItemDto`, `RestrictionDto`, `NumberDetailDto`, `TimelineEventDto`, `EvidenceSnapshotDto`, `OwnerInstructionDto` and corresponding lower-camel `*Schema` exports | Parse outbound DTOs; all row revisions come from storage. `ownerReadSchema` wraps data with `as_of` and `coverage`; `attentionPageDtoSchema` defines snapshot pagination. `assessmentDtoSchema` is the assessment contract. |
| E | `src/services/salesIntelligence/fixtures.ts`: `undatedFollowupFixture`, `ambiguousReviewFixture`, `closedReviewFixture`, `unknownCoverageFixture` | Review-only Attention rows may have `outreach: null`; do not invent an Outreach record or known coverage. |
| B–D | `src/models/<ModelName>.ts`: `get<ModelName>Model`, named schema and index exports; `src/models/salesIntelligence/registry.ts`: `CSI_MODEL_REGISTRY` | Accessors route through `getMongoDatabaseName`; never import a global default-DB model. Index migration must precede writes. |
| B–D | `src/services/salesIntelligence/transactions.ts`: `csiCas`, `executeCsiCommand`, `appendCsiAudit`, `CsiTransactionContext`, `payloadHash` | Use callback session for aggregate, audit, and downstream enqueue. External calls must remain outside retried transactions. |
| B–D | `src/services/salesIntelligence/jobs.ts`: `enqueueCsiJob`, `claimCsiJob`, `renewCsiJob`, `completeCsiJob`, `failCsiJob`, `JobInput`, `JobLease` | Mongo is authoritative; queue messages carry wake-ups. Commit effects inside completion callback, never before it. |
| D | `src/services/salesIntelligence/auth.ts`: `requireCsiRun`, `issueCsiRunToken`, `runClaimsSchema`, `RunClaims`; `evidence.ts`: `validateEnvelopeEvidence` | Load stored run/job; construct evidence manifest from trusted persisted snapshots. No model-provided manifest or actor. Full MCP tools remain D. |
| C, D | `src/services/salesIntelligence/aiBudget.ts`: `initializeCsiBudgetPeriod`, `reserveCsiBudget`, `reconcileCsiBudget` | C computes staffed/calendar boundaries; D reserves before provider calls and reconciles actual usage, including overestimates. |
| C, E | `src/services/salesIntelligence/policy.ts`: `defaultCsiPolicy`, `resolvePolicy`, `initializeCsiPolicy`, `updateCsiPolicy`; `settings.ts`: `readCsiSettings`, `commandCsiSettings` | Reads do not initialize storage. Owner HTTP GET/PATCH `/settings` use those commands. Env bootstraps first persist only; later Owner edits win. Kill switches stay env-only. Policy versions are append-only. |

## CSI-02 concrete imports (server-relative, September 17)

Capture contract version `csi-capture-v1`, built on `csi-foundation-v1`. Handoff: [evidence/csi-02/HANDOFF.md](evidence/csi-02/HANDOFF.md). Service card: `docs/knowledge/services/number-activity-capture.md`.

| Consumer | Import and exports | Obligation |
| --- | --- | --- |
| CSI-03 (webhook fan-out) | `src/services/numberActivity/observeWebhookEvents.ts`: `normalizeWebhookPartyObservations(payload, receivedAt)`, `observeRingCentralWebhookEvents(observations, deps)`, `SessionObservationResult`; `ObserveDependencies.request_id` | Call from the durable capture-projection job with stored receipt evidence, not from an unawaited route hook. Pass the durable job id (24-hex) as `request_id` so every audit row ties back to the job (otherwise rows carry `request_id_generated: true`). One result per telephony session; `ok:false` carries a bounded `error_code`, never a provider body. Account comes from party `accountId`/event path or `RINGCENTRAL_ACCOUNT_ID`; `account_unresolved` must stay visible, not defaulted. |
| CSI-03/A (cron) | `src/services/numberActivity/reconcileCallLog.ts`: `runCallLogReconcileOnce(deps?)`, `ReconcileSummary` (incl. `request_id`, `throttled_count`, `throttle_retry_after_ms`, `throttle_retry_after_observed`), `CALL_LOG_ALL_DIRECTIONS_SCOPE`, `callLogReconcileConfig()` | Returns `{skipped:true, skip_reason:"disabled"|"lease_held"}` when the flag is off or the lease is held; never throws provider content. A 429 anywhere in a run ends the run (no further gap repair). `throttle_retry_after_observed:false` means the wait is the documented default, not a provider header. Mount at `/api/cron/sales-intelligence-call-log-reconcile` (`3-59/10 * * * *`) with `CAPTURE_CALL_LOG`; registration is not done in CSI-02. |
| CSI-04, C, D, E | `src/models/CallInteraction.ts` rows: `external_endpoint_kind`, `parties[]` (role/extension/sequence/connected/terminal), `legs[]`, `legs_overflow_count` (monotone lower bound), `recordings[]` (all ids, `observed_at`, `lead_conversation_id` null until CSI-11), `sources[]`, `provider_last_modified_at`, `terminal`, `projection_revision`, `merged_into_id` | Follow `merged_into_id` to the canonical row before deriving; any recount or rebuild must filter `merged_into_id: null` because tombstones keep `contact_number_id` after their rollup contribution was removed. Count an interaction once; count recordings per `recordings[]` entry. `contact_type` is `unknown` unless provider voicemail, transcript or Owner set it. `direction: "Unknown"` is honest absence of provider direction, not Internal. |
| CSI-04, E (audit/history) | `sales_intelligence_audit_events` rows with `invalidation.kind: "interaction"`: `event_kind` `interaction.created|updated|merged`; `actor.request_id`; `current.proof_ref` (`webhook:<uuid[,uuid…]>` or `call_log:<record id>`), `current.input_kind`, `current.request_id_generated`, `current.aliases_added`, `current.merged_interaction_ids` | `proof_ref` is the provider evidence that caused the row; `actor.request_id` is the CSI-03 job id or the reconcile run id when `request_id_generated` is false. Alias rows keep the `proof_ref` that originally proved them; the merge proof is on the `interaction.merged` row only. |
| CSI-04, E | `src/models/SalesIntelligenceSyncState.ts` scope `call_log_all_directions`: `cursor.last_sync_from/to`, `known_complete_through`, `gaps[] {from,to,reason,opened_at}`, `last_run`, `consecutive_failures` | Coverage Watermark is `known_complete_through` plus `gaps`; anything after it or inside a gap is "not yet observed", never "no call". |
| CSI-06 (outreach), CSI-05 (attachment), CSI-11 (discovery) | `sales_intelligence_jobs` stages `outreach_ensure` (subject `number:<contact_number_id>`, `input_refs=[interaction_id]`, dedupe `csi:outreach_ensure:interaction:<id>:<revision>`), `attachment_refresh` (`csi:attachment_refresh:number:<id>:1`), `recording_discovery` (`csi:recording_discovery:interaction:<id>:recording:<rid>` and `...:pending`) | Claim through `claimCsiJob`; load the current interaction by `input_refs`, never trust job payload fields. A `:pending` discovery job means "terminal, no recording id observed yet", not "no recording". |
| Tests (B/C/D) | `src/services/numberActivity/fixtures.ts`: `webhookDelivery`, `inboundQueueAnsweredDeliveries`, `outboundUnansweredDeliveries`, `internalCallDelivery`, `withheldInboundDelivery`, `callLogRecord`, `inboundConnectedCallLog`, `transferredCallLog`, `outboundMissedCallLog`, `voicemailCallLog`, `internalCallLog`, `withheldMissedCallLog`, `malformedNumberCallLog`, `sessionIdOnlyCallLog`, `syntheticDirectory` | Synthetic only; not evidence of live provider shapes beyond the fields the projection reads. |
| Pure consumers | `src/services/numberActivity/interactionProjection.ts`: `fromWebhookParties`, `fromCallLogRecord`, `mergeProjections`, `sameProjection`, `aliasesFor`, `TERMINAL_PARTY_STATUSES`, `CONNECTED_CALL_LOG_RESULTS`; `phone.ts`: `classifyEndpoint`, `toE164`, `toNationalTenDigit`; `directory.ts`: `buildDirectoryLookup`, `loadDirectoryLookup`, `EMPTY_DIRECTORY_LOOKUP` | No I/O in the projection; rebuild (CSI-04) may replay stored evidence through the same functions. |

Additive CSI-01 corrections recorded with this contract (tests in `scripts/test-csi-capture.replica.test.ts` and `src/services/numberActivity/*.test.ts`): `CallInteraction.external_endpoint_kind` (nullable enum over `CONTACT_NUMBER_KINDS`); audit `invalidation.kind` gains `"interaction"` in `src/models/salesIntelligence/infrastructure.ts` and `appendCsiAudit`. Both are optional/enum-widening; existing CSI-01 tests still pass (15/15 replica, 69/69 focused). Not changed: qualification services, `ringcentral_call_log_sync_state`, webhook route, `vercel.json`, subscriptions. Independent review resolutions (additive, September 17 later): [evidence/csi-02/INDEPENDENT-REVIEW.md](evidence/csi-02/INDEPENDENT-REVIEW.md).

## CSI-03 concrete imports (server-relative, September 17)

Fan-out contract version `csi-fanout-v1`, built on `csi-foundation-v1` and `csi-capture-v1`. Handoff: [evidence/csi-03/HANDOFF.md](evidence/csi-03/HANDOFF.md). Service card: `docs/knowledge/services/sales-intelligence-webhook-fanout.md`.

| Consumer | Import and exports | Obligation |
| --- | --- | --- |
| CSI-04, C (`outreach_ensure`, `attachment_refresh`), CSI-11 (`recording_discovery`) | `src/services/numberActivity/jobDispatch.ts`: `dispatchCsiWakeup(payload, deps)`, `defaultStageHandlers(capture)`, `StageHandler`, `parseCsiWakeup`, `csiWakeupSchema` | Register a consumer by adding `<stage>: (jobId) => yourWorker(jobId)` to the handler map (the queue consumer passes `deps.handlers`); until then wake-ups for your stage return `no_consumer` and the job stays `pending` with `attempts` untouched. Your worker must call `claimCsiJob(owner, jobId, ttl, "<stage>")` so it never leases another stage's work. Payload is `{ job_id }` only. |
| CSI-04, C, F | `src/routes/sales-intelligence-cron.routes.ts`: `createSalesIntelligenceCronRouter(deps)`, `CSI_CRON_PATHS`, `requireCronAuth`; `deps.extraRecovery: [{ name, flag, run }]` | Add a per-stage recovery step to `/api/cron/sales-intelligence-job-recovery` through `extraRecovery`; it runs only when its own `SALES_INTELLIGENCE_<flag>` is true. New crons go in `vercel.json` and this router; a handler file alone is not registration. |
| All workers | `src/services/salesIntelligence/jobs.ts` (CSI-01, additive): `claimCsiJob(owner, jobId?, ttlMs?, stage?)`; `completeCsiJob(lease, mutation, { result?, resultFrom? })`; `failCsiJob(lease, reason, retryAfterMs?, { result? })`; job row field `result` (bounded JSON, default null); audit `invalidation.kind` gains `"job"` | `result` is a bounded summary (counts, ids, bounded codes), never a provider body. `resultFrom` (CSI-04) derives that summary from the mutation return value inside the same completion transaction. Write a `job`-kind audit row inside the completion callback when a job's outcome must be visible in history. |
| E (Coverage), F | `src/services/numberActivity/webhookRecovery.ts`: `runReceiptWatermarkRecovery(overrides)`, `RecoverySummary` (`pages`, `scanned`, `created`, `existing`, `quarantined[]`, `budget_exhausted`, `watermark_before/after`, `error_code`), `receiptRecoveryConfig()`; sync-state scope `webhook_receipts` (`cursor.last_sync_to` is the monotone watermark) | `quarantined` receipt ids are deterministic failures an operator must look at; `budget_exhausted` means more receipts remain in the window and the next minute continues. `skip_reason: "disabled" \| "lease_held"` as elsewhere. |
| E (Coverage), F | `src/services/numberActivity/captureProjectionWorker.ts`: `runCaptureProjectionJob(jobId?, deps)`, `drainCaptureProjectionJobs(max, deps, { deadlineMs })`, `CaptureProjectionOutcome`, `CaptureProjectionJobResult`, `SessionSummary` | Job `result.results[]` is one entry per telephony session: `ok:true` with `interaction_id`, `contact_number_id`, `projection_revision`, `noop`, `created`, `newly_terminal`, `jobs`; or `ok:false` with `error_code` ∈ `account_unresolved \| account_mismatch \| identity_missing \| projection_failed \| persist_failed \| retry_exhausted`. The `job` audit row `capture_projection.completed` on `subject_key: job:<id>` carries the same summary. |
| Route (owned by CSI-03 edit) | `src/services/numberActivity/webhookFanout.ts`: `fanOutCaptureProjection({ receiptId, uuid, telephonySessionId }, deps)`, `FanoutResult`, `ensureCaptureProjectionJob`, `captureProjectionDedupeKey`, `publishCaptureProjectionWakeup`, `salesIntelligenceQueueTopic`, `CAPTURE_PROJECTION_STAGE` | Response field `captureProjection` on the webhook route: `skipped(flag_off \| receipt_not_durable \| no_telephony_session)`, `enqueued`, `existing`, `enqueue_failed`, `enqueue_timeout`. Only `enqueued`/`existing` mean a job exists now; the other two are closed by recovery. |
| F (deployment), ops | `src/services/ringcentral/webhook-subscription-lifecycle.ts`: `planAllDirectionSubscription`, `ensureAllDirectionSubscription`, `renewOwnedSubscription`, `repairOwnedSubscription`, `classifySubscriptions`, `subscriptionHealth`, `validationEchoHeaders`, `ringCentralSubscriptionProvider`, `mongoOwnershipStore`, `SubscriptionOwnershipError`, `SubscriptionOwnershipRecordError`, `SubscriptionOwnershipUnavailableError`; `webhook-subscriptions.ts`: `buildRingCentralTelephonyEventFilters("all")`, `listStoredRingCentralWebhookSubscriptionIds`, `markStoredRingCentralWebhookSubscriptionStatus` | Ownership is the stored metadata row; nothing not in it is ever renewed, deleted or replaced. `plan` is read-only. Creating the production all-direction subscription is a G6 deployment action for Team F through `ops/ringcentral/sales-intelligence-subscription.ts --action ensure --confirm-production-subscription`; it was not run. |
| `vercel.json`, `src/app.ts` | Crons `/api/cron/sales-intelligence-call-log-reconcile` (`3-59/10 * * * *`), `/api/cron/sales-intelligence-job-recovery` (`* * * * *`), `/api/cron/sales-intelligence-directory-sync` (`20 5 * * *`, handler in CSI-04); queue function `api/queues/sales-intelligence-consumer.ts` on `sales-intelligence-events*` | Wake-ups accelerate; the minute cron is the recovery path. Consumers re-register their own Mongo connection. |

Additive CSI-01 corrections recorded with this contract (tests in `scripts/test-csi-fanout.replica.test.ts` and `src/services/numberActivity/fanout.test.ts`): optional `stage` filter on `claimCsiJob`; `result` option on `completeCsiJob`/`failCsiJob` (both with `runValidators`); job schema field `result`; audit kind `job`. Qualification-owned additive edits: `captureRingCentralWebhookEvent` returns `receiptId`; `buildRingCentralTelephonyEventFilters` accepts `"all"` (inbound branches byte-for-byte unchanged). CSI-01 replica 15/15 and CSI-02 replica 12/12 pass unchanged. Independent review resolutions: [evidence/csi-03/INDEPENDENT-REVIEW.md](evidence/csi-03/INDEPENDENT-REVIEW.md).

## CSI-04 concrete imports (server-relative, September 17)

Number Activity reads contract version `csi-numbers-v1`, built on `csi-foundation-v1`, `csi-capture-v1` and `csi-fanout-v1`. Handoff: [evidence/csi-04/HANDOFF.md](evidence/csi-04/HANDOFF.md). Service card: `docs/knowledge/services/number-activity-reads.md`.

| Consumer | Import and exports | Obligation |
| --- | --- | --- |
| E (Owner Numbers/Search/Coverage), C (attachment/outreach reads later) | `src/services/numberActivity/dto.ts`: `numberSearchItemDtoSchema`, `numberSearchPageDtoSchema`, `numberDetailReadDtoSchema`, `numberTimelineEventDtoSchema`, `numberTimelinePageDtoSchema`, `numberRollupsDtoSchema`, `numberConnectionsDtoSchema`, `NUMBER_DETAIL_READ_ONLY_FIELDS`, `NUMBER_DTO_FIXTURES` | Parse outbound DTOs. Search items never include `search_terms` (Owner-only on detail). Full customer E.164 appears because these routes are Owner-only. `NUMBER_DTO_FIXTURES` are synthetic 555-01xx examples. Detail is a strict superset of the CSI-01 `numberDetailDtoSchema` data shape; `outreach_records`, `restrictions` and `review_items` are composed by the existing CSI-06 `readNumberOutreach` mapper; CSI-14 adds explicit nudge history and timeline invalidations. |
| E, C | `src/services/numberActivity/search.ts`: `searchNumberActivity(query)`, `numberSearchQuerySchema`, `parseSearchTerm`, `buildNumberSearchFilter`, `encodeNumberCursor`/`decodeNumberCursor` | Cursor-paginated (default 50, max 200) over `contact_numbers`. Completeness: `(last_activity_at DESC, _id DESC)` keyset; every match reachable exactly once. `q`: ≥10 digits → exact E.164; 3–9 digits → suffix on `digits_reversed`; otherwise anchored prefix on `search_terms` (names, Job Number, Agent). `hygiene` accepts only boolean / `"true"` / `"false"` (`z.coerce.boolean()` is forbidden — Zod 4 treats every non-empty string as true). Absent or `false` keeps the Owner default (`kind: "external"`); `true` shows only non-external kinds. `?hygiene=0`, `off`, empty, or any other string is `400 INVALID_INPUT`. Reads never mutate. |
| E, C | `src/services/numberActivity/contactNumbers.ts`: `getContactNumberDetail(id)`, `toNumberSearchItem`, `toRollupsDto` | `null` when the id is malformed or missing. `connections.interactions_total_recount` is a fresh `merged_into_id: null` count against `rollups.interactions_total`. All attachment states are returned. |
| E, C (outreach/nudge events) | `src/services/numberActivity/timeline.ts`: `getNumberTimeline(id, { cursor, limit }, { sources, extraSources })`, `TimelineSource`, `mergeTimeline`, `compareTimelineEvents`, `encodeTimelineCursor`/`decodeTimelineCursor`, `DEFAULT_TIMELINE_SOURCES` | Total order `happened_at DESC, kind ASC, id DESC`; `observed_at` is separate. Canonical interactions only (`merged_into_id: null`); recordings counted per `recordings[]` entry. Conversation link scan is cursor-aware (`started_at <= cursor.happened_at`, bound 2000). The CSI-06 `outreachTimelineSource` is registered in `DEFAULT_TIMELINE_SOURCES`; CSI-14 includes nudge invalidations in that source. `extraSources` remains an additive extension point. No body, transcript or summary text. `lead_messages.to` has no index in this patch (Team F migration); timeline message reads may collection-scan until then. |
| E (Coverage), F | `src/services/numberActivity/coverage.ts`: `readCaptureCoverage()`, `ownerRead(data, now?)`, `CoverageDto` | Honest: no `call_log_all_directions` state → `known_through: null`, `call_log: "unknown"`; open gaps are "not yet observed". `recording_content` stays `unknown` until CSI-11. A stream is `unavailable` when `consecutive_failures > 0` or `last_run.error_code` is set; `webhook: "ok"` requires a `webhook_receipts` row with `cursor.last_sync_to` and no failure (a lease row alone is not health). `call_log: "ok"` requires `known_complete_through` and no failure; a stale-but-healthy watermark stays `ok` and is judged by `known_through`. `ai_paused` is this dataset's budget-paused jobs only. Reads never upsert. |
| C (CSI-10 mapping), capture | `src/services/numberActivity/directorySync.ts`: `runDirectorySyncOnce(overrides)`, `normalizeDirectory`, `directoryDigest`, `DIRECTORY_SCOPE` (`"directory"`), `DIRECTORY_SNAPSHOT_BOUND` (30), `DirectorySyncSummary`; `directory.ts`: `loadDirectoryLookup` | Latest snapshot by `taken_at`; none → empty lookup, roles stay `unknown`, company classification is not guessed. Sync is flag `SALES_INTELLIGENCE_DIRECTORY_SYNC`, fenced lease, digest-deduplicated, bounded to the last 30 per account (raw-collection prune). A→B→A (unique digest already stored) advances `taken_at` on that row and reports `changed: true` so the lookup sees the current roster. `DirectorySyncSummary.truncated` is true when more than 200 queues existed (member lists past the cap are stored as `[]` — "not fetched", not "empty"; the snapshot document itself has no truncation marker). Rep mapping review is Team C; this module supplies effective-time participant evidence only. |
| E (rebuild command), F, job recovery | `src/services/numberActivity/rebuild.ts`: `enqueueNumberRebuild`, `enqueueRebuildAll`, `runRebuildJob`, `drainRebuildJobs`, `recountNumber`, `sameRebuiltFields`, `loadRebuildEvidence`, `REBUILD_STAGE` | Owner command only enqueues (`202` + `job_id`); worker recounts inside `completeCsiJob` under revision CAS. Filter `merged_into_id: null`. No new business facts (`classification`, eligibility, `last_meaningful_contact_at`, `running_summary` untouched). Activity bounds are **repair, not monotone widen**: `first_observed_at` / `last_activity_at` are replaced by the canonical interaction window (tombstones excluded); the `number.rebuilt` audit records both prior and current bounds because this can reorder search. No-op rebuild bumps no revision and writes no audit. Fan-out uses fixed `input_revision: 1` and treats `IDEMPOTENCY_CONFLICT` as existing so a retry after capture bumped a number does not abort. `enqueueRebuildAll` is a service/ops entry point (no Owner route in 04 §1) and has no resume cursor — a fleet too large for one invocation restarts from `_id` ascending. |
| E (routes) | `src/routes/sales-intelligence-admin.routes.ts`: `createSalesIntelligenceAdminRouter(deps)`, `CSI_ADMIN_PREFIX` (`/api/v1/admin/sales-intelligence`) | Mounted after the CSI boundary. Flag off → `404 FEATURE_DISABLED`. Owner-only; `Idempotency-Key` required on `POST /numbers/:id/rebuild`. Body is the closed `csiCommandSchema`; only `rebuild_number` is accepted here. |
| CSI-03 consumer / cron (this patch) | `jobDispatch.defaultStageHandlers` registers `rebuild`; job-recovery cron drains rebuild under `SALES_INTELLIGENCE_ENABLED`; `CSI_CRON_PATHS.directorySync` handler is `runDirectorySyncOnce` | Same `{ job_id }` wake-up contract as CSI-03. |

Additive CSI-01 correction recorded with this contract: `completeCsiJob(lease, mutation, { resultFrom })` derives the stored job `result` from the mutation return value inside the completion transaction (CSI-01/03 `{ result }` still works). CSI-01 replica 15/15, CSI-02 replica 12/12 and CSI-03 replica 11/11 pass unchanged. Independent review (Request Changes → Approve): [evidence/csi-04/INDEPENDENT-REVIEW.md](evidence/csi-04/INDEPENDENT-REVIEW.md).

### Storage and transaction invariants

Inventory: ContactNumber, CallInteraction, CallInteractionAlias, NumberLeadAttachment, RepIdentityLink, OwnerRepNudge, OutreachRecord, OutreachFollowup, IntelligenceRun, IntelligenceEvidenceSnapshot, IntelligenceSubmission, IntelligenceFinding, IntelligenceEffect, SalesIntelligenceOwnerInstruction, IntelligenceOwnerAssessment, SalesIntelligenceReviewItem, SalesIntelligenceContactRestriction, SalesIntelligenceJob, SalesIntelligenceAuditEvent, SalesIntelligenceCommandExecution, SalesIntelligenceAiBudget, SalesIntelligenceAiReservation, SalesIntelligencePolicyVersion, SalesIntelligencePolicyPointer, SalesIntelligenceAttentionSnapshot, RingCentralDirectorySnapshot, SalesIntelligenceSyncState, SalesIntelligenceSyncWindow; existing LeadConversation is extended compatibly.

ContactNumber is global normalized E.164. Provider interaction aliases and recordings are account-scoped. Unknown recording accounts block migration apply; they are never assigned a fabricated default. LeadConversation metadata can be null, legacy summary text still renders, and structured summaries map envelope `money_and_dates/commitments/discrepancies` to public `money_dates/promised/mismatch` using `envelopeSummarySections` and `renderEnvelopeSummary`.

Multiple active follow-ups are supported. `commitment_key` is a stable server-assigned obligation identity; nullable due dates stay null. Unique commitment/missed-episode fences do not implement semantic matching. Immutable snapshots, submissions, instructions, assessments, effects, policy versions and audit history are protected through model hooks. A finalized run cannot replace its analysis/prompt/manifest. Mutable review/projection state is separate. Privileged raw collection migration/retention access is outside these application hooks.

Owner command identity is `(actor_scope, idempotency_key)`. Hash includes command plus payload. Same key/payload replays the stored response; changed payload fails. The callback owns semantic preconditions and all aggregate revisions. CAS rejects stale revisions; audit and enqueue must share its session. CSI evidence remains in the CSI command/audit collections: no new official domain-command origin or EntityChange entity is introduced.

Jobs dedupe by stable key and payload hash. Claims increment lease epoch; completion checks owner, epoch and unexpired lease **after** the effect callback, inside the transaction. Failed fencing rolls back effects. Bounded retries distinguish permission/budget pauses from failures. Reservations atomically admit `actual + reserved + estimate <= ceiling`; reconciliation/release is exactly once. Budget increases resume budget-paused jobs in the current dataset; decreases do not.

### Authentication handoff

`src/routes/sales-intelligence-boundary.routes.ts` is mounted after the existing v1 guard. Owner reads/commands require the existing signed Registry Owner identity; broad API secret alone is insufficient. Internal run routes additionally require the named scoped key and `x-vantage-intelligence-run-token`. The named key has a hard internal-route allowlist even if its configured routes are broad. The token binds run, subject, allowed tools, audience, nonce, deployment/database, lease epoch and expiry (maximum 900 seconds), checked against the stored active run and lease. A token without the scoped key is denied. Feature handlers are intentionally left to B–D; mount them after this boundary.

Configure `SALES_INTELLIGENCE_SCOPED_KEY_NAME` to match an entry in existing `VANTAGE_SCOPED_API_KEYS`; set deployment identity and a dedicated run signing secret through the deployment secret store. Never embed credentials in DTOs. Existing Gateway/Blob and optional matched Redis URL/token configuration is reused by `csiProviderConfiguration`; TEST_MODE disables optional Redis. No provider capability is claimed by these accessors.

For that named key, configure these exact stable route templates: GET `/api/v1/internal/sales-intelligence/runs/:id/context`, POST `/api/v1/internal/sales-intelligence/runs/:id/read`, POST `/api/v1/internal/sales-intelligence/runs/:id/submit`, GET `/api/v1/internal/sales-intelligence/runs/:id/submission`. Only these templates expand a 24-hex run id; generic scoped keys retain literal-path semantics, and arbitrary wildcards are not supported. Each actual request still requires its matching signed run token.

`initializeCsiBudgetPeriod` stores `activated_at` and atomically resumes budget-paused jobs once when the supplied period is current and has allowance. Precreating a future period does not wake work; retrying activation does not repeatedly resume exhausted jobs. Job dedupe is database-global: a reused key under another deployment is an explicit `IDEMPOTENCY_CONFLICT`, never a successful replay of unclaimable work.

### Minimal request and receipt examples

Synthetic undated action (idempotency key is supplied separately to the command executor by the handler):

```json
{"command":"create_followup","expected_revision":1,"outreach_record_id":"aaaaaaaaaaaaaaaaaaaaaaaa","action":{"kind":"check_availability","description":"Check crew availability","due_at":null}}
```

Strict submission receipt shape (D persists and returns it atomically with application-job enqueue):

```json
{"run_id":"aaaaaaaaaaaaaaaaaaaaaaaa","submission_id":"bbbbbbbbbbbbbbbbbbbbbbbb","application_job_id":"cccccccccccccccccccccccc","status":"submitted"}
```

Owner intervention bodies preserve exact analysis targets. Digest equality, correction ownership and source availability are runtime checks, separate from shape validation:

```json
{"command":"confirm_run","expected_revision":2,"expected_output_digest":"synthetic-output-digest"}
```

```json
{"command":"apply_suggestion","expected_revision":2,"run_id":"aaaaaaaaaaaaaaaaaaaaaaaa","suggestion_output_digest":"synthetic-suggestion-digest","due_at":null,"responsible_agent_id":"bbbbbbbbbbbbbbbbbbbbbbbb"}
```

```json
{"command":"reanalyze","expected_revision":2,"mode":"original_evidence","source_run_id":"aaaaaaaaaaaaaaaaaaaaaaaa","owner_correction_ids":["bbbbbbbbbbbbbbbbbbbbbbbb"],"reason":"Review the Owner correction against original evidence"}
```

`source_run_id` is required for original-evidence mode; current-context mode may omit it. `owner_correction_ids` is an explicit array (empty when none). Optional suggestion overrides preserve the distinction between omission and explicit null.

`FindingDto.assertion` contains the discriminated envelope finding; review state/effect status are sibling projection fields. This explicit nesting replaces the earlier illustrative flattened DTO so assertion fields cannot be confused with mutable application state. Consumers must use the executable schema.

Defaults remain $80/month, Mon–Sat 08:00–20:00 America/New_York, 30/15 staffed-minute deadlines and 1,440 staffed-minute Going cold. Clock evaluation, call reconciliation, Outreach effect rules, full MCP tools and dashboard screens are downstream work. Scenario fixtures above remain integration acceptance requirements, not claims that those services exist.

## CSI-07 concrete imports / initial CSI-08 slice — September 19

`src/services/salesIntelligence/live.ts`: `watchCsiChanges`, `streamCsiInvalidations`, `CSI_LIVE_COLLECTIONS`; registered Owner GET `/api/v1/admin/sales-intelligence/live?scope=production`. Existing signed Owner + v1 authentication and ENABLED gates; no new flag/index/migration. Replica-set oplog changes over existing audit/projection collections coalesce into SSE invalidations. `event: invalidation`, `id: <connection>:<sequence>`, data `{version:1,reason:"connect"|"reconnect"|"change"|"clock",as_of,refetch:"all"}`. Cursor is advisory, not a durable business-event replay token: every connection refetches all active DTOs, including unknown/missed Last-Event-ID. Clock frames every 15s; watch/timer cleanup on disconnect/error/backpressure/240s lifetime; retry 1s. Feature disable closes the stream. No source documents on wire.

E: `/api/sales-intelligence-live` authenticates/refreshed host session, denies Admin, signs upstream Owner headers, forwards Last-Event-ID, streams without buffering and aborts upstream on cancel. JSON proxy forwards CSI mutation Idempotency-Key and preserves envelope-level `as_of`/`coverage`; `registry_code` remains normalized by the CSI client. Browser identity/API-secret headers are never trusted. CSI header/global search use fixed Current records without rewriting the shared preference.

Additive optional `OutreachDto.primary_number:{id:string,e164:string}|null`, populated from the primary Contact Number; old immutable snapshots can omit it. DTO reads remain read-only. UI uses current `data.items[].outreach/derived` and `data.outreach_records[]`; export types are not wire authority. No `/overview` call exists. CSI-08 remains **partial**, with first 50 Attention rows, server-supplied reasons, explicit pending/error/empty states, Number/Outreach read panels, nullable dates and independent owners. Review-only Lead rows without an Outreach are visibly unavailable until review integration. No commands or messaging controls are presented as functional.

Attention publication still depends on CSI-06 worker/recovery. HTTP/SSE never runs it. Local launcher runs it serially every five seconds only on `testvantagemovers_csi07preview`; production minute cron is unchanged. Hosting scale, deployed change-stream rights and production clock latency remain CSI-16 checks. See [intake](evidence/csi-07/INTAKE.md), [source hashes](evidence/csi-07/SOURCES.md), [handoff](evidence/csi-07/HANDOFF.md).

## CSI-08 operational UI continuation — September 19

Supersedes the partial UI description above. Admin consumes actual Number search/keyset and immutable Attention snapshot cursors; expiry clears only the Attention cursor. Review-only Lead subjects, attachment decisions and manual Lead selection, all existing follow-up/Outreach commands, restrictions, merged Number timeline and Reps read/review are connected to signed BFF routes. Export mocks/adapters are not wire contracts. Overview and Attention q/source_label remain absent; no invented counts or filtering.

Additive server read contracts: Outreach `allowed_actions` includes `create_followup`; Lead facts, latest canonical Number call and related-record links are nullable actual evidence. Attachment reads expose `allowed_actions`; Number detail exposes `attach_lead` with Number revision for an absent pair. Restrictions expose revision and `resolve_restriction` availability. Review GET accepts `subject_key,state,cause_kind` plus cursor/limit and rows expose `resolve_review`. Command service remains final authority, including unresolved identity and active-restriction checks.

Owner commands preserve one key and exact payload across unknown outcomes. Known revision conflicts retain local drafts; explicit acknowledgement advances the revision, and follow-up patch sends only fields edited from the original draft. Nullable dates remain nullable. Overall versus action assignment stays distinct. No-op clock scans do not increment revision; real transitions do. Accepted note/reason fields persist in audit history. First absent-pair attachment audits an explicit unlinked prior state, with no official-record write.

Official Admin destinations require `database_scope=production` (not the API `scope` parameter). Reps requires an explicit account ID with stored directory evidence; performance metrics remain unknown and messaging unavailable. CSI-14 dialogs and CSI-15 remain separate. CSI-09 Coverage/settings and Lead entry: [evidence](evidence/csi-09/INTAKE.md). [CSI-08 evidence](evidence/csi-08/HANDOFF.md).

## CSI-18 executable additive contracts — September 19, 2026

All reads/commands below are Owner-only, Current scope, through the existing signed BFF. `Idempotency-Key` is required for commands. Durable duplicate responses use `{data:{response,replayed}}`; changed payload under the same key and stale versions return 409.

| Route | Executable input / response |
| --- | --- |
| GET `/analysis-runs` | `contact_number_id` and/or `conversation_id`, `_id` cursor, limit 1–200. `data.items,next_cursor`. |
| GET `/analysis-runs/:id` | Optional `history_cursor`. Original output and output/suggestion digests, model/prompt, current/editable, finding revisions/effects, current actions, Outreach revision, Owner instructions with exact-version assessments, pending rerun requests, immutable `history,history_next_cursor`. |
| GET `/conversations/:id/findings` | Optional `run_id`; same analysis read contract. |
| GET `/analysis-runs/:id/evidence[/snapshotId]` | Manifest paging; snapshot membership checked server-side. Content is bounded to 16,000-character sections with offset cursor. Purged/missing payload returns unavailable, reason and nullable purge time; no current-data replacement. |
| POST `/analysis-runs/:id/confirm` | `confirm_run,expected_revision,expected_output_digest`. No effects. |
| POST `/findings/:id/confirm` | `confirm_finding,expected_revision,expected_output_digest`. Exact finding and parent output. |
| POST `/findings/:id/correct` | `correct_finding,expected_revision,expected_output_digest,replacement,target_effect_id,target_followup_id,reason`; optional `action_changes` (CSI-06 partial follow-up input), `expected_revisions` for affected follow-up, `reanalysis_mode`. Replacement retains finding key/kind. |
| POST `/findings/:id/retract` | `retract_finding,expected_revision,expected_output_digest,reason`, affected follow-up revision fences; optional `reanalysis_mode`. Blocked reversals are recorded. |
| POST `/analysis-runs/:id/apply-suggestion` | `apply_suggestion,expected_revision,run_id,suggestion_output_digest`, one Outreach revision fence, optional `due_at,responsible_agent_id` (nullable). Creates Owner-origin work. |
| POST `/analysis-runs/:id/reanalyze`, `/conversations/:id/reanalyze`, `/numbers/:id/reanalyze` | `reanalyze,expected_revision,mode,source_run_id,owner_correction_ids,reason`. Revision refers to the source run. Original mode requires source; current mode can resolve the published source from the target. Response 202 contains future `run_id,job_id,status:queued`. |

Correction/retraction default to current-context refresh after immediate mutation. Original-evidence unavailability does not roll back the correction. The UI renders original model assertions, effect outcomes, Owner decisions and assessments separately. Prior confirmations do not transfer to new assertions. Evidence references are AI-provided, not independently verified citations. Exact coverage and remaining acceptance are in CSI-18 CHECKS/REVIEW; this contract note alone does not close the issue.
