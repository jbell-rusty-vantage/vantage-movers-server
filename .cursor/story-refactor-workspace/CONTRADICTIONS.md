# Contradictions

Standing list. Do not silently merge sources. Not knowledge.

## Open

- Standalone GitHub checkout (`vantage-movers-server`) often has no workspace-root `CONTEXT.md` and no `docs/adr/`. Do not invent copies. Use `docs/knowledge/services/` and names already in the module.
