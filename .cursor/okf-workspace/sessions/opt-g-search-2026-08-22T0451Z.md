# Session opt-g-search-2026-08-22T0451Z

- Date (UTC): 2026-08-22T04:51Z
- Phase: optimization
- Unit started / ended: `g-search` / `g-search`
- Lock: taken
- Branch / PR: `docs/okf-optimization` / https://github.com/jbell-rusty-vantage/vantage-movers-server/pull/6

## Disk at start

- `OPTIMIZATION.md` next unchecked: this Cloud checkout was stale on `cursor/okf-documentation-optimization-1f56` / main (`opt-a`); disk on `origin/docs/okf-optimization` had `g-search` unchecked
- `.cursor/businesslogic/` exists?: no
- `pnpm okf:query --type Service` count: 40
- `pnpm okf:query --type Service --status deprecated` count: 0

## Units completed

- `g-search` → done
  - `form-lead-search.md` → changed
  - `call-lead-search.md` → changed
  - `lead-browse.md` → changed
  - `admin-search.md` → changed
  - `analytics.md` → changed

## Code-truth

- `form-lead-search.md` → Mongo `limit` before scoring; first/last name schema-legal but unused (`not_found`); CSV ObjectId `ref_no` skips search (no exact Tracking Reference lookup); `resolveGranotFormLead` / granot-match is exact ref then mongo id then this search (phone or email required, then source-gate + prior quoted tiebreak)
- `call-lead-search.md` → `normalizePhoneNumberForMatch` drops fewer-than-8-digit phones; name is unanchored (not lowercased); first/last only → `{ _id: { $exists: false } }`; no `normalized_job_no`; no dedicated service test
- `lead-browse.md` → `q` and `source_company` hit three source-label snapshots; `lead_source_company` exact ObjectId; `source_granularity_key` anchored; populate `receiver_agent.granot_crm_username`; form card uses `destination_zip`, no `local`
- `admin-search.md` → search fields include snapshots + `source_granularity_key`; combined flatten-then-`slice(limit)`; `mongoose.isValidObjectId` + `toObjectId` ORed with regex
- `analytics.md` → `derived_source_company` prefers `employee_source_snapshot` then form/call slugs then label snapshots then booking `source`; historical receiver-agent empty; lead cost excludes form duplicates + unmatched calls; agent deposit counted per unwind; CSV leaves-or-childless-company
- tests read: `formLeadSearch.service.test.ts`, `v1.validation.test.ts` (search/browse schemas), `admin.service.test.ts` (`globalAdminSearch`), `analytics.service.test.ts`
- routes: `v1.routes.ts` POST `/form-leads/search`, `/form-leads/granot-match`, `/call-leads/search`, GET `/form-leads`, `/call-leads`, `/admin/search`, `/admin/analytics/:report`, overview, agent-sales, analytics CSV

## Messages posted

- next-run: start `g-catalog`. Update PR #6. Do not removen.
- resolved prior next-run from `opt-g-sheets-2026-08-22T0354Z`

## Ideas parked

- none

## Contradictions

- `adr-skipped-absent` still open
- `ops-registry-authoritative-plan-absent` still open
- `public-v1-referral-cancel-vs-gated-release` still open (not this cluster)

## Next atomic unit (must match NOW.md)

- `g-catalog` — `catalog.md`, `testimonial.md`, `granot-http-collector.md`
