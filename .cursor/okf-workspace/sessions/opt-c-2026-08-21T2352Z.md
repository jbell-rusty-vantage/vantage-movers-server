# Session opt-c-2026-08-21T2352Z

- Date (UTC): 2026-08-21T23:52Z
- Phase: optimization
- Unit started / ended: `opt-c` / `opt-d`
- Lock: taken
- Branch / PR: `docs/okf-optimization` / https://github.com/jbell-rusty-vantage/vantage-movers-server/pull/6

## Disk at start

- `OPTIMIZATION.md` next unchecked: `opt-c` (this Cloud checkout was stale on main; disk on `origin/docs/okf-optimization` had A/B checked)
- `.cursor/businesslogic/` exists?: no (already deleted on the optimization branch)
- `pnpm okf:query --type Service` count: 36
- `pnpm okf:query --type Service --status deprecated` count: 0

## Units completed

- `opt-c` → done — `## Related businesslogic` → `## Related services`; old `.service.md` / `granotLifecycle.*.md` link labels rewritten. Leftover: Primary-code TypeScript paths `granotLifecycle.ts` and `granotLifecycle.validation.ts` still match the literal `granotLifecycle.` Done-when regex (shipped filenames, not conversion labels).
- `opt-d` → done — four fat routers thinned after reading form-lead / sheet-sync / google-sheets code + tests. Unique true facts moved into those Services. `generated.by: process:okf-docs-optimization` on the three Service bodies that changed.

## Code-truth

- `form-lead.md` → CRM `label` is `crm_label_snapshot`, not request `crm_company_label`; post-commit order is SMS → `finalizeSheetSync` → `submitFormLeadToCrm`; quiet-hours SID / 15-min–35-day window / re-read-after-accept / `scheduled` rank 1
- `sheet-sync.md` → queue publish needs test-runner off + `VERCEL=1` + `VERCEL_REGION` via `shouldPublishSheetSyncQueue()`; cron is `router.all`; crash heuristic `claimed > 0` and zero outcomes
- `google-sheets.md` → Master provisions `Duplicate Calls`; no `Bad Calls` write path; `GETMOVERS_LEADS_SHEET_ID`; `paid_overflow` has no source container
- tests read: `formLead.service.ts` + `formLeadPayload.test.ts` + `leadMessaging.service.test.ts` + `quietHours.test.ts` + `sheetSync.test.ts` + `runtime.test.ts` + `drainer.test.ts` + `jobPlanner.test.ts` + `targets.test.ts`

## Messages posted

- next-run: start `opt-e` (coverage-gap Services; code + tests first)
- resolved prior next-run from `opt-a-2026-08-21T2254Z`

## Ideas parked

- none new

## Contradictions

- `adr-skipped-absent` still open
- `ops-registry-authoritative-plan-absent` still open
- added `public-v1-referral-cancel-vs-gated-release`

## Next atomic unit (must match NOW.md)

- `opt-e` — Coverage gaps: lead-messaging, employee-bookings, reporting, ingestion (or documented skip)
