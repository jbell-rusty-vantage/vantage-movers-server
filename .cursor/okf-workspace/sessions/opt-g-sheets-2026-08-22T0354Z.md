# Session opt-g-sheets-2026-08-22T0354Z

- Date (UTC): 2026-08-22T03:54Z
- Phase: optimization
- Unit started / ended: `g-sheets` / `g-sheets`
- Lock: taken
- Branch / PR: `docs/okf-optimization` / https://github.com/jbell-rusty-vantage/vantage-movers-server/pull/6

## Disk at start

- `OPTIMIZATION.md` next unchecked: this Cloud checkout was stale on `cursor/okf-documentation-optimization-f7fb` / main (`opt-a`); disk on `origin/docs/okf-optimization` had `g-sheets` unchecked
- `.cursor/businesslogic/` exists?: no
- `pnpm okf:query --type Service` count: 40
- `pnpm okf:query --type Service --status deprecated` count: 0

## Units completed

- `g-sheets` → done
  - `sheet-sync.md` → changed
  - `google-sheets.md` → changed
  - `domain-commands.md` → changed
  - `sheet-sync-process.mdc` → env-only: non-preview `VERCEL_ENV` publish gate + admin retry drain (link left in the Service)

## Code-truth

- `sheet-sync.md` → `shouldPublishSheetSyncQueue` also requires non-preview `VERCEL_ENV`; `SHEET_SYNC_QUEUE_LOCAL_PUBLISH` does not enable publish; admin retry starts `runSheetSyncDrain("admin")` (default `failed` only) and does not publish; `delete_booked_lead` supersedes `booked_lead` only, not `booking_chain`; unmatched Call skip is on both legacy `syncSourceLead` and queued planner; lease heartbeat + timeout→pending vs crash→retrying
- `google-sheets.md` → call sync always deletes the stale Calls/Duplicate Calls tab; queued Bad Leads delete only when `sheet_sync[]` has the target; `WRITE_SOURCE_LEAD_SHEETS` is literal `"true"` only; form source label uses CRM/granularity snapshots then helper; empty `ref_no` → `"not provided"`
- `domain-commands.md` → idempotency is `existing:{command}:{submission_id}` else `request:{command}:{requestId}`; `confirmGranotBooking` is not on `canonicalDomainCommands`; employee actors cannot build existing-write context; Granot admin initiator accepted; `*InTransaction` must not nest txn/finalize (test-enforced)
- tests read: `sheetSyncOutbox.service.test.ts`, `sheetSync.test.ts`, `jobPlanner.test.ts`, `adminSheetSync.service.test.ts`, `targets.test.ts`, `domainCommands.test.ts`, `entityChange.test.ts`, `runtime.test.ts`
- routes/queue: `sheet-sync-cron.routes.ts`, `api/queues/sheet-sync-consumer.ts`, `v1.routes.ts` admin sheet-sync

## Messages posted

- next-run: start `g-search`. Update PR #6. Do not removen.
- resolved prior next-run from `opt-g-bookings-2026-08-22T0254Z`

## Ideas parked

- none

## Contradictions

- `adr-skipped-absent` still open
- `ops-registry-authoritative-plan-absent` still open
- `public-v1-referral-cancel-vs-gated-release` still open (domain-commands points at it; did not merge gated Release)

## Next atomic unit (must match NOW.md)

- `g-search` — `form-lead-search.md`, `call-lead-search.md`, `lead-browse.md`, `admin-search.md`, `analytics.md`
